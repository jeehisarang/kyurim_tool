# 작업지시서 — 환자 대면 페이지 하단 고정 CTA 바 (단일 버튼으로 통일)

## 배경
경쟁사 앱 참고 — 스크롤은 본문만 되고 전환버튼은 하단에 항상 고정. 다만 
버튼 종류를 프로그램/이벤트로 나누지 않고 **"진료상담문의하기" 하나로 통일**.

## 적용 대상
- `/p/[token]` — 프로그램티칭지
- `/s/[token]` — 티칭지+이벤트 통합페이지
- `/refer/trial`, `/refer/trial/[token]` — 체험 신청페이지 (신청 버튼은 
  성격이 다르므로 유지, 이건 하단고정 CTA와 별개)
- `/m/[token]` — 검토 후 필요시 제외 (기존 지시서와 동일 방침)

## 변경 사항 (기존 지시서 대비)
- 기존 "프로그램문의하기"/"이벤트문의하기" 2개 버튼 → **"진료상담문의하기" 
  1개 버튼으로 통합**
- 클릭 동작은 기존과 동일(카카오채팅 오픈 + 콜백 업무 자동생성) 유지, 
  다만 생성되는 업무 제목을 페이지 성격 구분 없이 통일된 형태로 변경 
  (예: "{환자명}님 진료상담 문의 — 연락 필요")
- 기존 `requestConsultCallback()`/`requestEventInquiryCallback()` 두 함수를 
  하나로 합치거나, 공용 함수 하나로 라우팅 (내부적으로 어떤 페이지에서 
  눌렀는지는 로그에 남기되, 환자에게 보이는 버튼/문구는 항상 동일)
- `/s/[token]`처럼 프로그램+이벤트가 같이 있는 페이지도 버튼은 하나만 
  하단에 고정

## 구현 방향 (이전과 동일)
1. 공용 컴포넌트 `StickyBottomCta.tsx` — 버튼 하나, `position: fixed; 
   bottom: 0;`
2. 콘텐츠 영역 하단 padding으로 마지막 내용 안 가리게 처리
3. 모바일 폭 기준 확인

## 검증
- 스크롤 중에도 버튼 항상 고정 노출
- 클릭 시 카카오오픈+업무생성 정상 동작(회귀 확인)
- 콘텐츠 마지막 줄 안 가려지는지
- npx tsc --noEmit, npm run build 통과

## 완료 후
- 브라우저 확인 전 커밋/푸시 금지, 다른 지시서들과 함께 일괄 확인 후 커밋