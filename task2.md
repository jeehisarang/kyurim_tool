# 작업지시서 — 마감설문 작성 시 3만원 적립금 실제 지급 (누락 기능 구현)

## 배경
`/refer/exit/[prescriptionId]` 페이지 문구에는 "마감설문 작성 시 3만원 적립"이 
이미 명시되어 있으나, 실제로 이 크레딧을 지급하는 로직이 구현되어 있지 않음. 
문구와 실제 동작을 일치시킬 것.

## 조사 먼저 (구현 전 확인)
1. `/refer/exit/[prescriptionId]` 페이지 실제 텍스트에서 "3만원"이 정확히 
   어떤 문구로 어디에 쓰여있는지 확인 (완료 전 안내문인지, 완료 후 화면인지)
2. 기존 `ReferralCreditEntry`의 `source` enum에 이런 완료성 보상 항목이 
   이미 있는지(빠뜨린 케이스인지) 확인

## 구현 내용
1. `ReferralCreditEntry.source`에 `EXIT_SURVEY_COMPLETION` 추가
2. 마감설문(`ExitSurveyResponse`) 제출 처리 로직에서, 해당 처방(prescriptionId)에 
   대해 **이전에 이미 이 source로 지급된 적이 없으면** 3만원 CONFIRMED 
   크레딧을 즉시 생성 (idempotent — 같은 처방으로 중복 제출/재제출해도 
   한 번만 지급되도록 반드시 체크)
3. 금액(3만원)은 하드코딩보다 `/settings/trial-campaign`의 기존 금액 설정 
   화면에 필드로 추가해서 원장님이 조정 가능하게 (기존 5,000/35,000/70,000원 
   설정과 같은 방식)
4. 신규 확정 크레딧이므로 최근 도입한 **1년 유효기간** 규칙 동일 적용
5. 완료 화면(설문 제출 후) 문구를 실제 지급된 금액과 동적으로 일치시킬 것 
   (하드코딩된 "3만원" 텍스트 금지, 설정값을 변수로 참조)
6. `/refer/my/[token]` 잔액에도 정상 합산되는지 확인

## 소급 적용 여부 — 확정
- **소급 지급하지 않음.** 이 지시서 반영 이후 신규 마감설문 제출건부터만 적용
- 김미숙님 등 기존에 이미 제출된 건들은 손대지 말 것

## 검증
- 새 테스트 환자로 마감설문 제출 → 3만원 CONFIRMED 크레딧 정상 생성 확인
- 같은 처방으로 재제출(테스트) 시 중복 지급 안 되는지 확인
- `/settings/trial-campaign`에서 금액 조정 시 완료화면 문구도 함께 바뀌는지 확인
- `/refer/my/[token]` 잔액 반영 확인
- 기존(반영 이전) 제출건들의 크레딧이 그대로인지(변화 없는지) 확인
- npx tsc --noEmit, npm run build 통과

## 완료 후
- 브라우저 확인 전 커밋/푸시 금지, 다른 작업들과 함께 일괄 확인 후 커밋