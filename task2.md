# 작업 지시서 — 프로덕션 모드 전환 검토 (다운타임 최소화)

## 배경
추천링크가 실제로 외부에 퍼지고 있어, 작업 중 발생하는 dev 서버 재기동
다운타임이 실제 신청 기회 손실로 이어질 수 있는 상황이 됨. 현재 원장실 PC에서
`npm run dev`로 운영 중인 구조를 점검하고, 가능하면 프로덕션 모드로 전환해
다운타임을 줄이는 방안을 검토한다.

## 확인/검토 사항
1. 현재 실행 방식 확인 (npm run dev로 계속 떠 있는 게 맞는지)
2. 프로덕션 빌드(`npm run build` + `npm start`)로 전환 시:
   - 장점: 코드 변경 감지/핫리로드가 없어 훨씬 가볍고 안정적으로 떠 있음,
     재시작 자체도 dev보다 훨씬 빠름
   - 단점: 코드 수정할 때마다 build를 다시 돌려야 서버에 반영됨(지금도
     매번 npm run build는 하고 있으니 큰 차이 없을 수 있음 — 확인 필요)
3. 다운타임을 아예 없애는 방법(더 나아간 옵션, 여유 있을 때 검토):
   - PM2 같은 프로세스 매니저로 서버를 관리해서 "무중단 재시작"이 가능한지
   - 혹은 재시작 중 잠깐이라도 "점검중" 안내 페이지가 뜨게 하는 게 가능한지
     (완전 다운보다는 나은 사용자 경험)
4. 위 검토 결과를 바탕으로, 지금 당장 적용 가능한 가장 간단한 개선안부터 제안

## 보고
- 현재 방식과 프로덕션 전환 시 차이 (다운타임 체감 감소 정도 예상)
- 전환에 따르는 리스크나 번거로움이 있다면 솔직히 공유
- 당장 적용 vs 나중에 여유 있을 때 적용, 원장님이 판단하실 수 있게 정리