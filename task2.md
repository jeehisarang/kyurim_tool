# 작업지시서 — 업무(WORK) 항목 내용 읽기전용 미리보기 팝업

## 배경
지금은 업무 등록 시 입력한 상세 내용을 확인하려면 "수정" 버튼을 눌러 수정모드로
들어가야만 볼 수 있음. 실사용 중(임애리 실장) 직접 불편함이 보고됨 — 등록만 해두면
내용을 그냥 확인할 방법이 없음.

## 요구사항

### 1) 클릭 대상
업무(WORK) 타입 항목의 "할일종류"(제목) 텍스트를 클릭 가능하게 변경
(현재 일반 텍스트 → 클릭 가능한 링크 스타일로)

### 2) 팝업 내용
클릭 시 읽기전용 팝업(모달)으로:
- 제목
- 내용(상세 설명)
- (있다면) 담당자, 마감일도 참고용으로 함께 표시 — 단, 이 팝업에서는 어떤 것도
  수정 불가, 순수 조회 전용
- 팝업 하단에 "수정하기" 버튼 — 누르면 기존 수정 모달/화면으로 바로 연결
  (기존 수정 로직 재사용, 새로 만들지 않음)

### 3) 적용 범위
`/home` 화면과 `/todo` 화면 둘 다 반영. 두 화면이 업무 목록 UI 구현이
서로 다르므로(사전 확인 필요 — 각 화면의 실제 컴포넌트 구조 확인 후 각각 적용),
각 화면에 맞는 방식으로 동일한 팝업 컴포넌트를 재사용해서 넣을 것
(신규 팝업 컴포넌트는 공용 1개로 만들어 양쪽에서 import)

### 4) 제목 길이 처리
"할일종류" 컬럼에 제목이 길게 나올 경우(예: "지불보증서연장 (자율업무)"처럼) 컬럼
안에서 잘리는 경우가 있을 수 있음 — 말줄임(ellipsis) 처리 적용하고, 잘렸든
안 잘렸든 클릭하면 팝업에서는 항상 전체 제목이 다 보이도록 처리

### 5) 톡/처방 타입은 건드리지 않음
이번 변경은 "업무(WORK)" 타입에만 적용. 톡(TALK)/처방(PRESCRIPTION) 타입의
기존 "톡 관리" 버튼 동작은 그대로 유지.

## 검증 요구사항
- `/home`, `/todo` 양쪽에서: 제목 클릭 → 팝업 정상 오픈 → 내용 정상 표시 → 팝업 내
  수정하기 버튼 → 기존 수정화면 정상 연결 확인
- 긴 제목 케이스(테스트 데이터로 재현) 컬럼 말줄임 + 팝업 전체 노출 확인
- 팝업에서 실수로 값이 바뀌거나 저장되는 경로가 없는지 확인(읽기전용 보장)
- 기존 "체크"/"삭제"/"수정" 버튼 동작 회귀 없음 확인
- npx tsc --noEmit / npm run build 통과

## 완료 후 보고
- 두 화면 각각 실제 적용 방식 요약(공용 컴포넌트 구조)
- 브라우저 확인 결과(팝업 오픈/내용표시/수정연결)
- 빌드 통과 여부