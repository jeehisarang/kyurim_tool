# 작업지시서: 1단계 조사 결과 반영 — 결정사항 확정

## 결정 1: 성능개선 (배치 사전계산) — 승인, 같이 진행

- 신규 테이블: `DashboardStatsSnapshot` (ADD-only, 기존 로직 영향 없음)
- 새벽 배치(cron 또는 기존 스케줄러 패턴 재사용)로 하루 1회 `computeDashboardStats()` 결과를 미리 계산해 저장
- `/api/dashboard`는 실시간 계산 대신 최신 스냅샷을 읽어서 반환하도록 변경
- 기존 `computeDashboardStats()` 함수 자체는 그대로 유지 (배치가 이 함수를 호출해서 저장하는 방식 — 로직 중복 구현 금지)
- 스냅샷 생성 시각(예: 새벽 3시 등 환자 없는 시간대)과 실패 시 재시도/알림 여부는 기존 배치 작업 패턴(검사 리마인더 등)과 동일하게 처리
- 대시보드 화면에 "최종 업데이트: MM/DD HH:MM" 같은 작은 타임스탬프 표시 추가 (스냅샷 기반이라는 걸 사용자가 인지할 수 있게)

## 결정 2: 4단계 "발송건수" 정의 — 2번(근사치) 채택

- 추가 로그 테이블 없이, **PatientShareLink / PatientTeachingPage row 생성 건수**를 "발송 건수"로 간주
- 클릭 여부 판단은 `firstViewedAt` 유무로 (viewCount 아님 — viewCount는 중복방문도 카운트되므로 "클릭했는지 여부" 판단엔 부적합)
- 지표 계산: 기간 내 생성된 링크 수 대비, 그 중 firstViewedAt이 존재하는(=한 번이라도 열어본) 비율
- 화면 문구에 "근사치" 뉘앙스를 살짝 남기는 것 권장 (예: 툴팁 "링크 생성 후 열람 비율" — "발송 후 클릭률"이라는 확정적 표현보다 완곡하게)
- 이 지표도 위에서 만든 기간선택 드롭다운(7일/30일/이번달) 적용

## 진행 순서 제안
1. 배치 사전계산 구조부터 구현 + 확인 (대시보드 전체가 이 위에서 동작하므로 먼저)
2. 이전 지시서의 2~3단계(월별 누적환자수 콤보차트, 예약율/인당내원수 기간선택 통합)
3. 4단계(링크 클릭률, 근사치 방식)

## 주의사항
- 스냅샷 배치 실패 시 폴백: 스냅샷이 없거나 오늘 것이 없으면 기존 실시간 계산으로 자동 폴백 (화면이 빈 값으로 뜨는 것보다 느리더라도 실시간 계산이 나음)
- 테스트는 기존 원칙대로 테스트 환자/합성 데이터 사용 후 정리
- 브라우저에서 실제 스냅샷 타임스탬프, 콤보차트, 기간선택, 링크클릭률 전부 시각 확인 → tsc/build 통과 확인 → 커밋/푸시