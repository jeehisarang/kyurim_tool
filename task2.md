# 작업지시서 — 미션톡 주간 발송/수행 통계 요약카드

## 배경
`/missions/today`(오늘의 미션 발송) 페이지에 발송상태(발송됨/미발송) 컬럼은 이미
구현되어 있음. 여기에 "이번주(또는 선택 기간) 동안 몇 명에게 발송했고, 몇 명이
실제로 미션을 수행했는지" 요약 통계 카드를 페이지 상단에 추가한다.

## 사전 확인 (구현 전 조사 먼저)
아래 내용을 먼저 코드/스키마로 확인하고, 확인된 실제 필드명 기준으로 구현할 것:
1. `MissionDailyAssignment`에 발송여부/발송일시를 나타내는 필드명 확인
   (예: sentAt, sentBy 등 — 화면의 "발송됨"이 어떤 필드로 판정되는지)
2. `MissionSubmission`이 어떤 필드로 특정 날짜의 assignment와 연결되는지 확인
   (assignmentId 등 FK 확인)
3. 킬팻캡슐 진행중 환자 필터링 로직이 현재 어디 있는지 확인 (재사용 목적)

## 요구사항

### 1) 위치
`/missions/today` 페이지 상단, "오늘의 미션" 카드 위(또는 바로 아래)에 요약카드
1개 추가.

### 2) 기간 선택
- 기본값: 이번주(월~일, KST 기준) 고정
- 기간 선택 UI(날짜 범위 피커 또는 간단한 시작일/종료일 입력) 추가해서
  임의 기간도 조회 가능하게
- 페이지 진입 시 항상 "이번주"로 초기화

### 3) 집계 로직
선택된 기간(기본 이번주) 동안:
- **발송 인원수**: 해당 기간 내 `MissionDailyAssignment`에서 발송 처리된
  건의 **고유 환자 수** (한 환자가 여러 날 발송받았어도 중복 카운트 안 함 —
  "몇 명에게" 이므로 인원 기준)
- **수행 인원수**: 위 발송 대상 중, 해당 기간 내 자신에게 배정된 미션에
  대해 `MissionSubmission`이 존재하는 **고유 환자 수** (승인 완료 여부와
  무관하게 "제출 자체"를 수행으로 카운트 — 승인 대기 중인 것도 수행에 포함.
  승인 여부는 기존 `/missions/approvals`에서 별도로 확인 가능하므로 이 카드에서는
  다루지 않음)
- **수행율** = 수행 인원수 / 발송 인원수 × 100 (%, 소수점 없이 반올림)
- 프로그램(체험/1개월/3개월) 구분 없이 전체 통합 1세트만 표시

### 4) 카드 UI
- 3개 숫자 나란히: "발송 N명 / 수행 N명 / 수행율 N%"
- 기간 표시 텍스트 포함 (예: "7/21(월) ~ 7/27(일)")
- 기존 카드 스타일(색/폰트) 그대로 재사용, 신규 디자인 시스템 도입 금지

## 제약사항
- 스키마 변경은 위 사전확인에서 부족한 필드가 발견된 경우에만 ADD COLUMN으로,
  최소한으로
- 라이브 환자 데이터로 테스트하지 말고 테스트 환자(ID 426) 또는 dry-run 데이터로 검증
- 기존 발송/미발송 컬럼, 문구생성 버튼 로직은 건드리지 않음

## 완료 후 보고
- 실제 사용한 필드명/쿼리 로직 요약
- 이번주 기준으로 실제 화면 캡처 또는 값 확인 결과
- `npm run build` 통과 여부