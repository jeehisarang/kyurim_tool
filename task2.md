# 작업 지시서 — 신청 응답 상세화면에 추천인 실명 표시

## 배경
신청 응답 상세화면(`/refer/applications` 상세뷰)에 "추천코드"만 표시되어,
직원이 이 코드가 누구 것인지 바로 알 수 없음. 내부 확인 편의를 위해 추천인
실명을 같이 보여줄 필요가 있음.

## 수정
- TrialApplication 상세뷰에서 `referralToken`이 있는 경우, 해당 토큰으로
  ReferralLink → patientId → 환자 이름을 조회해서 "추천코드" 표시 옆(또는
  아래)에 함께 노출
  - 표시 형식 예: `추천코드: PJ0EQVkZ (김우석님)`
  - 링크가 만료됐거나 비활성 상태여도 이름은 그대로 보여줄 것(내부 확인
    목적이므로 유효성과 무관하게 항상 표시)
  - 혹시 토큰에 해당하는 ReferralLink를 못 찾는 예외 케이스(삭제된 환자 등)는
    "추천코드: PJ0EQVkZ (환자 정보 없음)"처럼 안전하게 처리

## 검증
- 실제 추천코드로 들어온 신청 건에서 추천인 이름이 정확히 표시되는지 확인
- 추천코드 없는(원내 QR) 신청 건은 기존처럼 추천코드 항목 자체가 안 보이는지 확인
- npx tsc --noEmit / npm run build 통과

## 완료 후 보고
- 수정 파일 경로
- 커밋 해시, git push 결과