# 작업 지시서 — 기존 진행중 환자 대상 MAIN 추천링크 소급 승격(Backfill)

## 배경
tier 승격 로직(promoteOrIssueMainReferralLink)은 "본프로그램 치료처방 등록" 이벤트
발생 시점에만 실행되도록 구현되어, 이 기능 배포 이전에 이미 킬팻캡슐 1개월/3개월
프로그램에 등록된 기존 환자들은 MAIN 링크가 생성되지 않은 상태로 확인됨.
다음주부터 기존 진행중 환자들에게 추천 이벤트를 안내할 예정이므로, 일회성
백필 스크립트로 소급 반영 필요.

## 작업 내용

### 1. 대상 환자 조회
- 현재 활성 상태(진행 중, 종료되지 않은)인 킬팻캡슐 1개월 또는 3개월 처방을
  보유한 모든 환자 목록 조회
- "활성 상태"의 기준: 종료일이 지나지 않았거나, 아직 마지막 처방 회차가 끝나지
  않은 처방 (기존 치료처방 로직에서 "진행중" 판정 기준 그대로 재사용)

### 2. 소급 승격
- 대상 환자별로:
  - 기존 ReferralLink가 있으면 → kind를 MAIN으로 승격, expiresAt을
    "오늘 기준" +3개월(1개월 프로그램) 또는 +6개월(3개월 프로그램)로 설정
    (원래 로직처럼 "등록일 기준"이 아니라 "백필 실행일 기준"으로 계산 — 이미
    한참 진행중인 환자에게 등록일 기준으로 계산하면 만료가 임박하거나 이미
    지났을 수 있으므로)
  - 기존 ReferralLink가 없으면 → 신규 발급 (TRIAL 단계 없이 곧바로 MAIN으로 생성)
- 처리 결과를 로그로 남길 것 (환자명/ID, 처리 전후 상태, 신규발급 여부)

### 3. 안전장치
- 스크립트 실행 전 대상 환자 목록과 예상 처리 내역을 먼저 출력(dry-run)해서
  원장님 확인 받은 뒤, 별도 확인/승인 후 실제 반영하는 2단계로 구현
- 이미 MAIN으로 승격된 환자(이번 백필 이전에 신규 발급 로직으로 이미 처리된 경우)는
  건드리지 않고 스킵
- 실행은 반드시 실데이터 대상이지만, 이 작업은 "조회 후 특정 필드만 갱신"하는
  단순 UPDATE라 사고 위험은 낮음. 그래도 실행 직전 dev.db 백업 권장

## 검증
- dry-run 결과 목록에 박순정/이은경/인정아 등 현재 화면에서 확인되는 진행중
  환자들이 정확히 포함되는지 확인
- 실제 반영 후 몇 명 샘플로 처방상세 화면에서 링크가 MAIN으로 바뀌고 만료일이
  정상 계산됐는지 확인
- 새로 등록되는 환자에 대한 기존 로직(등록 시점 자동 승격)이 이 백필과 충돌
  없이 계속 정상 동작하는지 회귀 확인

## Git 커밋 및 푸시
- 백필 스크립트는 일회성이므로 scripts/ 폴더 등 명확히 구분되는 위치에 저장
- git add . / commit -m "기존 진행중 환자 MAIN 추천링크 소급 백필 스크립트 추가" / push

## 완료 후 보고
- dry-run 결과 (대상 환자 수, 목록)
- 실제 반영 후 결과 (처리된 환자 수, 신규발급/기존승격 각각 몇 건)
- 검증 결과