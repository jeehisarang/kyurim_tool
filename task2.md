# 작업 지시서 — 체험 신청폼에 성별/출생년도(나이 자동계산) 필드 추가

## 배경
킬팻캡슐 처방은 성별/체질에 따라 갈리는 부분이 있어, 기존 신청폼(이름/연락처/키체중/
체중변화/약물/병력 등)에 성별과 나이 정보가 빠져있던 것을 보완.

※ 이 작업은 현재 진행 중인 "추천링크 tier 승격" 작업과는 별개 파일(신청폼 컴포넌트)을
건드리는 작업이므로 병행 가능. 단, 같은 파일을 동시에 수정하지 않도록 대상 파일 먼저
확인할 것.

---

## 1. 대상 화면
- 체험 신청폼 (`/refer/trial` 및 `/refer/trial/[token]` — 첨부 이미지의 "기본 정보" 섹션)

## 2. 필드 추가

### 2-1. 성별
- "이름/연락처" 다음, "키/체중" 앞에 배치
- 라디오 버튼 또는 토글 형태로 "남 / 여" 2개 선택지 (필수 입력)
- 기존 폼 스타일(브랜드 컬러, 카드형 선택지 등 이미 적용된 디자인 톤)에 맞춰 구현

### 2-2. 출생년도 → 나이 자동계산
- 성별 옆 또는 바로 아래에 "출생년도" 입력 필드 추가 (4자리 숫자 입력, 예: 1990)
- 입력 즉시(onChange) `현재년도 - 출생년도`로 만 나이 근사 계산해 입력창 옆/아래에
  "만 O세"로 실시간 표시
- 서버에는 출생년도 원본 값과 계산된 나이 둘 다 저장 (재계산 가능하도록 원본 보존,
  단 나이 자체는 표시용 파생값이므로 매번 조회 시 재계산해도 무방 — 서버 계산값을
  신뢰 소스로, 클라이언트 계산은 미리보기 용도로만 사용)

## 3. 스키마 변경
- TrialApplication(또는 해당 신청 모델)에 필드 추가:
  - `gender`: enum/string ("MALE" | "FEMALE")
  - `birthYear`: number
- 마이그레이션은 ADD COLUMN 방식으로만 (기존 데이터 보존, 신규 필드는 nullable 또는
  기본값 처리하여 기존 신청 레코드와 하위호환 유지)

## 4. 검증 (테스트 전용 데이터로)
- 테스트 신청 건으로 성별/출생년도 입력 → 나이 자동계산 표시 정상 확인
- 신청 완료 후 DB에 gender, birthYear 정상 저장 확인
- 신청 목록/상세화면(원장님이 보는 신청 리스트)에서 성별/나이가 노출되는지 확인
  (필요 시 목록 화면에도 컬럼 추가)
- 기존(필드 추가 전) 신청 레코드 조회 시 에러 없이 정상 표시되는지 회귀 확인

## 5. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "체험 신청폼: 성별/출생년도(나이 자동계산) 필드 추가"
- git push

## 완료 후 보고
- 변경된 파일/마이그레이션명
- 검증 결과
- git 커밋 해시, push 성공 여부