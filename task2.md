# 작업 지시서 — 백필 스크립트에 "특정 환자 지정" 옵션 추가

## 배경
인정아님처럼 킬팻캡슐 본프로그램이 이미 종료(STOPPED)되어 자동 승격 대상에서
빠졌지만, 실제로는 추천 활동을 활발히 하실 만한 분(빅마우스)에게 예외적으로
MAIN 추천링크를 만들어드리고 싶은 경우가 있음. 임의로 새 치료처방을 등록하는
것은 스케줄/통계 왜곡 부작용이 있어 지양하고, 기존 백필 스크립트에 "특정
환자 지정" 옵션을 추가해 안전하게 처리한다.

## 작업 내용
- scripts/backfill-main-referral-links.ts에 옵션 추가:
  - 기존 동작(활성 처방 전체 대상)은 그대로 유지 (기본값)
  - 신규 옵션: --patient=<환자ID 또는 차트번호> (쉼표로 여러 명 가능)
    지정 시, 처방 상태(진행중/종료 여부)와 무관하게 해당 환자에게만
    MAIN 링크를 발급/승격 (환자가 과거에 본프로그램 이력이 있었는지 정도만
    확인, 없으면 에러로 안내)
  - expiresAt 계산 로직은 프로그램 종류(1개월/3개월)에 따라 기존과 동일하게
    적용, 실행일 기준으로 계산

## 안전장치
- dry-run 우선 (기존 스크립트 패턴 유지), 확인 후 --apply로 실제 반영
- 지정 환자가 이미 MAIN 링크를 보유 중이면 스킵하고 로그로 안내

## 검증
- 테스트 환자로 --patient 옵션 단독 실행 → dry-run 결과 확인 후 실반영
- 인정아님 실제 반영 (dry-run 먼저 확인 후 승인받고 --apply)
- 기존 전체 백필 동작(옵션 없이 실행)이 이번 옵션 추가로 영향받지 않았는지 회귀 확인

## Git 커밋 및 푸시
- git add . / commit -m "백필 스크립트: 특정 환자 지정 옵션(--patient) 추가" / push

## 완료 후 보고
- 인정아님 dry-run 결과 및 실반영 결과 (링크/만료일)
- 회귀 확인 결과
- git 커밋 해시