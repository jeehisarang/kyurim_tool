# 작업지시서: (1) 리스트 key 중복 경고 수정 + (2) 추천링크 DB 저장 상태 점검

## 1. React key 중복 경고 수정

- 브라우저 콘솔에 "Encountered two children with the same key, `8`" 경고 발생
- 최근(치료처방 완료버튼/수동완료 전환) 작업에서 수정한 목록 렌더링 컴포넌트들 확인
- key로 쓰이는 값이 프로그램 id가 아니라 currentRound 같은 회차 숫자거나, 서로 다른 프로그램인데 같은 값을 key로 재사용하고 있는지 확인
- 원인 찾아서 고유값(예: 처방 레코드의 실제 id)으로 key 수정

## 2. 추천링크(킬팻캡슐 리퍼럴) DB 저장 상태 점검

- 최근 생성된 추천링크(ReferralLink 또는 관련 테이블) 레코드를 최신순으로 조회해서 아래 보고
  - 최근 생성 건수 (예: 최근 7일)
  - 정상적으로 token, 만료일, 생성자(환자/추천인) 정보가 다 채워져 있는지
  - null이거나 비어있는 필드가 있는 레코드가 있는지
- 최근 실패 로그(에러 발생 후 저장 안 된 흔적, 서버 로그의 에러 스택) 있는지 확인
- 프론트엔드에서 링크 생성 요청을 보냈는데 서버 응답이 실패(4xx/5xx)로 온 경우가 최근 있었는지 서버 로그 확인
- 이상 없으면 "정상 저장 확인" 결과만 간단히 보고, 이상 있으면 구체적으로 어떤 케이스인지 보고 (수정은 별도 승인 후)

## 참고
- 이전 세션에서 아직 답변 못 받은 것도 같이 확인 부탁: "완료" 버튼(수동완료 전환, 작업지시서 2번/3번)이 실제로 반영됐는지