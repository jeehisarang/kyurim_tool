# 작업 로그

## 2026-07-07

오늘 세션에서 진행한 작업을 시간 순으로 정리. 각 항목은 별도 `task.md` 지시로 진행되었고, 매번 `npm run build` 통과 확인 후 커밋함.

### 1. AI 문구생성 엔진: Anthropic SDK → OpenAI SDK 교체
- `src/lib/ai-message.ts`가 `@anthropic-ai/sdk` 대신 `openai` 패키지를 쓰도록 교체 (원장님이 다른 프로젝트에서 쓰던 OpenAI 크레딧 재사용 목적)
- 모델은 `gpt-4o-mini`로, 상수로 분리해 추후 교체 용이하게 함
- 기존 DAY2/DAY7/THIRD_VISIT 프롬프트 내용·함수 시그니처는 변경 없이 유지, `.env`도 `OPENAI_API_KEY`로 교체

### 2. 홈 화면(`/home`) 신설 + 대시보드 추이 차트 + 오늘할일 진행 요약
- 루트(`/`) 리다이렉트를 `/visit-check` → `/home`으로 변경
- 홈 화면: 요약카드 4개 + "이번달 흐름" 달력 그리드(일별 내원수/예약율에 따라 색상 진하기 표시)
- `/dashboard`에 recharts 기반 "일별 내원수·예약율" 콤보 차트(막대+꺾은선) 추가
- `/todo`에 "이번주 처리 현황 N/M건" 진행률 바 추가

### 3. 가상 환자 60명 + 내원기록 시드 데이터
- `prisma/seed-demo.ts` 신규 (기존 `seed.ts`와 별도, 재실행 방어 로직 포함 — 이미 실행됐으면 스킵)
- 진료분야 분포(급여치료 70%/자보 15%/기타 15%), 예약율(47.9% 근사), 일부 환자 다회 내원 등 실데이터 패턴 반영

### 4. 홈 화면 날짜 탐색 기능
- 상단에 "◀ 2026년 7월 7일 (화요일) ▶ + 오늘" 날짜 이동 바 추가
- `/api/visits`에 `?date=` 파라미터 추가(기존 호출과 하위호환), 카드/목록이 선택 날짜 기준으로 갱신
- 달력 그리드 클릭 = 날짜 선택 상태 공유 (같은 `selectedDate` 상태 사용)

### 5. 톡 할일 통합 + 용어 통일 (2일톡/7일톡/3회톡)
- `TodoTask` 스키마 확장: `prescriptionId`/`staffUserId` nullable화, `patientId` 추가 → 처방 할일과 톡 할일을 한 모델에서 표현
- `src/lib/talk-todos.ts` 신설: 2일톡(초진/재초진 다음날)·7일톡(마지막 내원 +7일)·3회톡(누적 3회 내원) 자동생성, `MessageLog` 발송 여부를 유일한 진실 원천으로 삼아 중복 생성 방지
- `/todo`, `/messages` 표시 라벨을 "2일톡/7일톡/3회톡"으로 통일 (DB enum 값은 그대로 유지)
- `/todo`에서 체크 ↔ `/messages`에서 발송확인이 서로 자동 동기화되는 구조 완성

### 6. 오늘할일 밀림/오늘 구간 분리 + 2일톡 생성로직 진단
- **진단**: 2일톡이 35건 쌓여 보이던 원인을 조사 → 생성 로직 버그 아님. dueDate가 "내원일+1"로 정확히 고정 저장되고 있었고, 단지 지난 dueDate 건들이 체크 안 된 채 누적된 정상적인 밀림이었음
- `/todo`를 "📌 밀린 할 일"/"📅 오늘 할 일" 두 섹션으로 분리, 밀린 항목엔 "N일 지남" 배지 표시
- `/home` 미리보기는 오늘 마감 항목을 우선 채우도록 수정(밀린 일로 전부 채워지지 않게)

### 7. 7일톡 스킵 + 환자메모 누적구조 + AI 프롬프트 재구성
**버그 3건 발견 및 수정** (이번 작업 진행 중 확인):
1. `/api/visits`가 `visitDate`를 자정 시각과 **정확 일치**로 조회하는데, 시드 데이터가 임의 시각으로 들어가 있어 "오늘 체크된 내원 목록"이 0건으로 잘못 표시됨 → 시드 스크립트를 자정 고정으로 수정
2. "이번주 처리 현황" 집계가 `TodoTask.isDone`만 보는데, 톡 할일은 애초에 이 필드를 쓰지 않는 구조라 톡 완료 건이 진행률에 반영되지 않음 → `MessageLog.sentDate` 기준으로 톡 완료를 별도 집계하도록 수정
3. `/api/messages/confirm`이 `aiDraftContent`를 안 넘기면 기존 저장된 AI 초안을 `null`로 덮어써버리는 문제 → 값이 없으면 기존 값을 유지하도록 수정 (`/todo`에서 톡 체크 시 초안이 사라지지 않게)

**신규 기능**:
- `PatientNote` 모델 신규 (환자 1명당 여러 건 누적, `Patient.memo`와 별개) — `/visit-check` 목록의 "메모+" 아이콘, `/messages` 톡 생성 카드에서 즉석 추가 가능
- `MessageLog`에 `skippedAt`/`skippedByUserId` 추가 → 완료/보류/미처리 3분류. 7일톡에 한해 "보류" 버튼 제공, 보류해도 재생성되지 않음
- `/messages` 톡 생성 패널 개선: 누적 메모 목록 → 추가 키워드 입력 → (3회톡만) 호전도(상/중/하) 드롭다운 → 문구생성
- **AI 프롬프트 구조 논의 및 재구성**: 기존 GPT 프로젝트(1년 전, 5단계 대화형: 시작→톡종류선택→환자정보입력→문구생성→수정확인)의 few-shot 예시(`docs/kakao-prompt-original.md`)를 그대로 반영해, 버튼 클릭 시 이미 톡종류/환자정보가 정해져 있으므로 1회 호출로 완성 문구를 뽑는 구조로 재설계. 2일톡/7일톡 톤을 명확히 분리(7일톡은 "오랜만의 안부, 걱정" 톤으로 재설계)하고 3회톡은 호전도별 A/B/C 구조를 few-shot으로 학습시킴
- 실제 GPT 호출로 검증: 누적 메모 중 관련 있는 것만 선별 반영되는지, 3회톡 상/중/하가 예시와 톤/구조가 일치하는지 확인 완료

## 2026-07-08 (야간, 집)

실제 코딩 없이 설계·논의만 진행한 세션(2시간 제한). 다음 세션에서 구현할 작업지시서 2건을 확정해 둠.

### 1. 환자용 티칭지 (신규 개념, 설계만 완료)
- 13-7(내부 스태프용 환자 통합뷰)과는 별개로, "환자에게 공개되는 URL 페이지" 개념 신설. 카드뉴스 형태, 톡 메시지에 링크로 자연스럽게 삽입되어 발송, 목적은 전환율/매출 증대
- 신규 모델 구상: `PatientTeachingPage`(token 랜덤고유값, patientId, contentJson 스냅샷, viewCount, firstViewedAt 등)
- 공개 라우트 구상: `/p/{token}` (인증 없음, 한지·청자색 톤 카드뉴스)
- 생성 트리거: 담당자가 톡생성기에서 수동 버튼으로 생성 (자동생성 아님)
- 콘텐츠: 문구카드(AI 공감형) + 데이터카드(검사수치 변화 등) 혼합
- 접속 제한: 만료 없음으로 결정 (부하 문제 아니고 노출기간 리스크 문제로, 현재 콘텐츠 성격상 리스크 낮다고 판단 — 추후 필요시 만료일 컬럼 확장 가능하게만 설계)
- 이번 세션에서는 설계만, 구현은 다음 세션 이후

### 2. 이벤트-톡 자연스러운 연계 아이디어 (설계 방향만 논의, 구현 시점 미정)
- 기존 문제의식: 상시 이벤트 문자 발송에 반응이 거의 없어 피로도만 누적
- 새 방향: 이벤트를 별도로 살포하지 말고, 이미 나가는 톡(2일톡/7일톡/3회톡 등)이나 검사 시점에 자연스럽게 카드 하나로 얹어서 전달
- 예: 근력검사 재검에서 이전 대비 체중 유의미한 증가 발견 시, 그 사실을 개인화 카드로 만들어 진행 중인 이벤트(예: 킬팻캡슐 3일체험)와 자연스럽게 연결
- 규림한의원 주요 프로그램(~10개) 전체를 한번에 설계하지 않고, 우선 1개 파이프라인(킬팻캡슐)만 검증 후 확장하기로 함
- 조건 매칭은 단순 규칙(예: 검사 2회 이상 + 특정 수치 변화폭 이상)으로 시작, AI 추천 로직은 이후 단계로 미룸
- 자동발송 아님 — 담당자에게 "후보 제안"만, 최종 발송 판단은 사람이 유지
- 요구사항 문서에 신규 섹션(14번 등)으로 기록 필요, 구체 설계/구현은 다음 세션

### 3. 검사(Examination) 기능 스키마 최종 확정 (실물 인바디/근감소증표 확인 후)
- Patient 모델에 height(키) 고정값 필드 추가 결정
- 인바디(BodyCompositionRecord): 핵심 입력 3개(체중/체지방율/복부지방율=WHR)로 단순화, VFA·부종지수는 제외
- 인바디에 사지근육량(오른팔/왼팔/오른다리/왼다리) 선택 입력 필드 추가 — 입력 시 근력검사와 동일한 공용 SMI 계산 함수로 SMI 자동 산출
- BMI는 별도 저장 없이 체중+키로 화면에서 실시간 계산해 표시만
- 근력검사(StrengthTestRecord)는 기존 구조 유지, SMI 계산 로직을 인바디와 공유
- 검사종류는 확장 가능한 목록으로 관리하되, 계산식/판정 있는 검사(현재 2종)는 코드로 관리, 향후 계산식 없는 단순 수치기록형(혈액검사 등) 필요 시 별도 범용 테이블 추가 예정 (지금은 만들지 않음)
- 환자 목록(치료처방/내원체크)에서 "검사" 버튼으로 바로 입력 가능하도록 UX 변경 (기존 독립페이지 재검색 흐름에서 전환)

### 검증
- 이번 세션은 논의만 진행, 코드 변경 없음

### 다음 세션 진행할 것
1. **최우선**: 검사(Examination) 기능 재설계 구현 — 위 3번 스키마/UI 그대로 작업지시서로 전달 예정 (이 채팅에서 이미 확정된 지시서 존재)
2. 환자용 티칭지(PatientTeachingPage) 구현 착수 여부 논의
3. 이벤트-톡 연계 파이프라인 구체 설계 (킬팻캡슐 1개 사례부터)
4. Windows 서버 자동시작 설정 마무리 (계속 이월 중)
5. "미션 배정형 업무관리" 기능 설계 논의 (계속 이월 중)
6. 로드맵 13-7 (환자 통합뷰, 그래프 시각화) — 계속 대기 중
7. git commit/push 상태 재확인 (계속 이월 중)

## 2026-07-13 (원장실)

> 이 세션 로그는 별도 파일로 남겨지지 않아, 커밋 이력(7개, 15:41~19:29) 기준으로 재구성함. 이후 세션부터는 이 파일에 바로 이어서 기록할 것.

이벤트 이미지/공유링크 계열 기능을 이어서 확장한 세션. 티칭지 생성 "유실" 버그와 공개페이지 정보노출 버그, 2건의 실사용 버그를 발견 즉시 수정.

### 1. 톡생성기(2일차/7일차/3회차) AI 프롬프트에 최근 상담기록 반영 (`7196e9f`)
- 티칭지 프롬프트와 동일 원칙 — 상담기록이 해당 톡 맥락과 관련 있을 때만 자연스럽게 반영, 무관하면 생략. ConsultationNote 없는 환자는 기존처럼 PatientNote만으로 정상 생성(회귀 없음)

### 2. 환자별 통합 공유링크(`/s/{token}`) 기능 신설 + 티칭지 생성 "유실"버그 근본 수정 (`240b048`)
- 유실버그 근본원인: DB 저장(`POST /api/teaching-pages`)은 항상 즉시 성공하지만 생성 결과(링크)가 컴포넌트 로컬 state에만 존재해 조회 수단이 전혀 없었음 — 탭 전환/이탈 시 DB엔 남아있어도 링크를 다시 찾을 방법이 없던 "UI 접근성 유실"이었지 DB 유실이 아니었음. `GET /api/patients/[id]/teaching-pages`로 상시 목록 조회 가능하게 해 근본 해결
- `PatientShareLink` 모델 신설(teachingPageId/eventImageId 조합, 동일 조합이면 재사용) — 공개 페이지 `/s/[token]`에서 티칭지+이벤트 통합 렌더링
- `ShareLinkPanel` 컴포넌트 신설, 톡생성기(TalkStudioPanel)에 삽입 — 링크는 편집 중인 초안에 끼워넣지 않고 복사 시점에만 결합(재생성/재편집해도 중복·꼬임 없음)

### 3. `PatientTeachingPage` 소프트삭제 기능 추가 (`de9e07f`)
- `isActive` 필드 추가. 생성화면 드롭다운에서만 제외되고, 이미 발송된 공개 링크(`/p/[token]`, `/s/[token]`)는 `isActive`와 무관하게 계속 정상 렌더링

### 4. [긴급] `/s/[token]` 공유링크 페이지 정보노출 버그 수정 (`2147ad6`)
- 근본원인: `AppShell.tsx`의 공개페이지 판별 로직이 `/patient-view`, `/p/`만 검사하고 신설된 `/s/`를 빠뜨려, 공개 공유링크 페이지에 실제 환자 이름이 노출되는 사이드바+실시간 활동피드가 그대로 렌더링되던 버그
- 확인 결과 기존 `PatientShareLink` 5건은 전부 원장님 본인이 생성 직후(수십초 내) 직접 열어본 자체 테스트로 파악됨 — 외부 환자 발송 정황 없음

### 5. `/s/[token]` 이벤트 섹션에 카톡 본문(finalCopy) 텍스트 추가 표시 (`b2a36f6`)
- 기존엔 합성 이미지(title+intro)만 노출했는데, 카톡 발송용 본문 전체를 이미지 아래 카드형 텍스트박스로 추가("완성된 이미지 그대로 사용" 모드 포함, finalCopy 빈 문자열이면 블록 생략)

### 6. 공유링크 토큰 단축 — UUID 36자 → nanoid 8자 (`ce4c8dd`)
- 카톡 발송 시 가독성 개선 목적. 기존 발급된 UUID 토큰 레코드는 건드리지 않음(마이그레이션 없음, 조회 경로가 토큰 값을 그대로 매칭하므로 형식 혼재돼도 문제없음)

### 7. 링크 자동첨부 시 고정 안내문구 추가 — 프로그램티칭/이벤트/통합 3종 (`0911bd0`)
- `ShareLinkPanel`이 URL과 함께 선택된 모드도 부모에 전달하도록 확장, 톡 복사 시 모드별 고정 템플릿(환자 이름만 치환, AI 호출 없음)을 본문과 URL 사이에 삽입. "링크만 복사" 버튼은 URL 단독 복사로 그대로 유지

### 검증
- 각 커밋마다 `npx tsc --noEmit`/`npm run build` 통과 확인 후 커밋

## 2026-07-13 (야간, 집)

GitHub에서 원장실 작업(위 7개 커밋)을 pull로 동기화(마이그레이션 2건 적용, 기존 환자 데이터 정상 유지 확인)한 뒤 이어서 진행. 이번 세션에서 만든 코드 변경은 `901ce6b` 커밋 1건(기능 2개 포함), push까지 완료.

### 1. 공유링크 복사 시 안내문구 자동첨부 — 톡 관리(TalkGroupManager) 화면에 누락돼 있던 것 발견해 추가
- 개별 환자 AI생성 화면(TalkStudioPanel)에는 원장실에서 이미 구현돼 있었음(`0911bd0`)을 코드로 재확인. 반면 "오늘 할 일 → 톡 관리"로 들어가는 `TalkGroupManager` 화면에는 "링크 포함하기" 섹션 자체가 아예 없다는 걸 발견
- `ShareLinkPanel`은 이미 `patientId`/`onLinkGenerated` props만으로 동작하는 범용 컴포넌트였어서 별도 추출 없이 그대로 재사용. `SHARE_LINK_INTRO` 안내문구 템플릿만 `TalkStudioPanel` 로컬 상수였던 걸 `ShareLinkPanel.tsx`로 옮겨 export해 공용화
- `TalkGroupManager`의 체크리스트와 "선택 문구 생성" 버튼 사이에 배치, `handleCopy`에 안내문구+URL 결합 로직 추가
- 실사용 데이터(환자 정원화, 등록된 이벤트)로 Playwright 실제 브라우저 검증: 4개 모드 노출, 프로그램티칭 "새로 만들기" 인라인 생성, 클립보드 복사 결과에 환자이름 치환+안내문구+URL 정확히 포함(`...정원화님을 위한 특별한 혜택을 아래 링크에서 확인해보세요 🙂\n<URL>`) 확인. 기존 개별 환자 화면 쪽 회귀 없음도 재확인

### 2. 상담모드(`/consult-mode`) 환자 선택 시 작성 내용 유실 버그 발견 및 수정
- 원장님 실사용 중 재현: 환자 미선택 상태로 상담 원문 입력 + AI 차팅변환까지 받은 뒤 환자를 선택하면, 방금까지 작성한 원문/변환결과가 통째로 사라지는 버그
- 원인: `selectPatient()`/`clearSelectedPatient()` 두 함수 모두 환자 선택·변경 시점에 무조건 `resetForm()`을 호출해, 그 안의 `rawText`/`convertedText`까지 함께 초기화되고 있었음("이력 스택 로드" `useEffect`는 `notes`만 건드려 문제 없었음 — 의심했던 지점 중 하나였으나 아니었음)
- 수정: 두 함수의 `resetForm()` 호출을 제거하고, 막 해소된 안내 메시지(`convertError`/`saveError`)만 지우는 `clearTransientErrors()`로 교체. "저장" 성공 시에는 기존처럼 `resetForm()`으로 전체 초기화 유지
- 케이스4(다른 환자로 변경 시 작성 내용 유지 vs 초기화)는 유지하는 방향으로 구현 — 저장 후에만 초기화됨
- 4가지 케이스 모두 원장님 브라우저 확인 완료

### 커밋
- `901ce6b` 톡관리 화면 링크포함하기 기능 추가 + 상담모드 환자선택시 내용유실 버그 수정 (push 완료, `0911bd0..901ce6b`)

### 검증
- `npx tsc --noEmit`, `npm run build` 통과
- 원장님 브라우저에서 두 기능 모두 확인 완료 후 커밋/push 진행
