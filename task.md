# 작업지시서 — 적립금 사용처리(ReferralCreditUsage) 수정/취소 기능

## 배경
`/settings/referral-credits`에서 "사용 처리"를 누르면 되돌릴 방법이 없었음. 
실수 방지를 위해 이미 처리된 사용내역을 금액/날짜까지 수정 가능하게 하고, 
완전히 잘못 눌렀을 경우를 위한 취소(삭제)도 함께 지원.

## 구현 내용
1. **내역 목록(각 환자 "내역 N건" 펼침 화면)에 항목별 "수정"/"삭제" 버튼 추가**
2. **수정**: 클릭 시 금액/날짜 입력 가능한 폼(모달 또는 인라인) → 저장 시 
   `ReferralCreditUsage`의 amount/usedAt 갱신
3. **삭제**: 클릭 시 확인창("이 사용내역을 삭제하시겠습니까? 삭제하면 
   해당 금액만큼 잔액이 복구됩니다") → 승인 시 레코드 삭제(또는 
   isCancelled 플래그 처리 — 아래 감사이력 방식과 함께 결정)
4. **감사 이력(중요)**: 금액이 오가는 기능이라, 수정/삭제 시 "누가 언제 
   원래 무엇을 어떻게 바꿨는지" 최소한의 로그를 남길 것 — 예: 
   `ReferralCreditUsage`에 `editedAt`/`editHistory`(JSON, 이전 값 기록) 
   필드 추가, 또는 별도 감사로그 테이블. 기존 `ActivityLog` 패턴 재사용 
   가능한지 먼저 검토
5. **잔액 재계산**: 기존 잔액 계산 함수가 사용액 합계를 그대로 다시 
   읽어오는 구조라면, 수정/삭제 후 별도 트리거 없이 자동으로 최신 
   잔액에 반영되는지 확인 (구조상 당연히 그래야 함 — 확인만)
6. **권한**: 원장 계정만 가능하게 할지, 사용처리를 하는 직원 누구나 
   가능하게 할지 — 기존 "사용 처리" 버튼과 동일한 권한 수준 유지 
   (원장 전용이면 그대로, 아니면 그대로)

## 검증
- 임의로 사용처리 하나 만든 뒤, 금액/날짜 수정 → 잔액 정확히 재계산되는지
- 같은 건 삭제 → 잔액이 삭제 전 금액만큼 정확히 복구되는지
- 감사 이력이 실제로 남는지(누가/언제/무엇을 바꿨는지 확인 가능한지)
- 여러 건 있는 환자(임애리, 12건)로 특정 1건만 수정해도 나머지 11건 
  안 건드려지는지
- npx tsc --noEmit, npm run build 통과

## 완료 후
- 브라우저 확인 전 커밋/푸시 금지, 다른 작업들과 함께 일괄 확인 후 커밋