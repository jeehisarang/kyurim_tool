# 작업 지시서 — 환자별 적립금 현황 한눈에 보기 + 사용 처리 + 개인화면 내역 표시

## 배경
지금은 환자의 적립금이 "체험 확정 건수/금액", "MAIN 확정 건수/금액"으로 흩어져
있어 실제 잔액(쌓인 것 - 쓴 것)을 한눈에 볼 수 없음. 또한 적립금을 실제로
사용했을 때 이를 기록할 방법이 없어, 사용 후 잔액이 자동으로 줄어들지 않음.
직원이 수동으로 "OO원 사용"을 입력하는 방식으로 구현한다.

## 1. 데이터 모델 — 적립금 사용 내역 (신규)
- 신규 모델(가칭 ReferralCreditUsage):
  - patientId
  - amount (사용 금액, 양수로 저장)
  - memo (선택, 예: "1개월 결제 시 사용")
  - 처리자(staffId), 처리일시
- 기존 확정된 적립(체험 CONFIRMED 5,000원, MAIN CONFIRMED 35,000/70,000원 등)은
  그대로 "적립 내역"으로 취급하고, 위 신규 테이블은 "사용 내역"만 담당
  (적립과 사용을 같은 테이블에 부호로 섞지 않고 분리 — 각각의 출처가 다르므로
  명확히 구분되는 게 유지보수에 유리)

## 2. 잔액 계산
- 환자별 적립금 잔액 = (체험 CONFIRMED 합계 + MAIN CONFIRMED 합계) − (ReferralCreditUsage 합계)
- 계산 함수 하나로 통일해서(getPatientCreditBalance(patientId) 같은 형태) 아래
  2개 화면에서 동일하게 재사용

## 3. 화면 A — 전체 환자 적립금 현황 한눈에 보기 (신규 또는 기존 화면 확장)
- 기존 /settings/referral-credits 화면을 확장하거나, 신규 목록 화면으로 구현:
  - 환자명 / 총 적립액 / 총 사용액 / 잔액 순으로 테이블 표시
  - 검색(이름/차트번호) 가능하게 (오늘 추가한 공통 검색 패턴 재사용)
  - 잔액 있는 환자만 필터링하는 옵션 있으면 좋음
- 각 환자 행에 "사용 처리" 버튼 → 클릭 시 금액/메모 입력하는 간단한 모달/폼
  → 제출 시 ReferralCreditUsage 레코드 생성
- 잔액보다 큰 금액을 사용 처리하려 하면 경고 표시(막지는 않되, "잔액 초과"
  경고 문구 정도)

## 4. 화면 B — 환자 개인 추천현황 화면(/refer/my/[token])에 사용내역 추가
- 기존 "확정 적립금: N건·O원" 표시 아래에:
  - "사용 내역" 섹션 추가: 사용일자 / 사용금액 / 메모 리스트
  - "현재 잔액" 크게 표시 (총 적립 − 총 사용)
- 이 화면은 환자 본인이 보는 화면이므로, 메모에 직원 이름 등 내부용 정보는
  노출하지 말 것 (사용일자/금액/메모 정도만)

## 5. 검증 (테스트 전용 환자로)
1. 테스트 환자로 체험+MAIN 적립 합쳐 10만원 확정 상태 만들기
2. 전체현황 화면에서 잔액 10만원 정상 표시 확인
3. "사용 처리"로 5만원 사용 입력 → 잔액 5만원으로 정상 갱신 확인
4. 환자 개인화면(/refer/my/[token])에서도 "현재 잔액 5만원" + 사용내역 1건
   정상 노출 확인
5. 잔액 초과 사용 시도(예: 남은 5만원인데 10만원 사용 입력) → 경고 표시 확인
   (막을지 경고만 할지는 구현 후 실제 화면 보고 원장님이 결정하셔도 됨)
6. 기존 확정/미확정 적립 표시 로직에 영향 없는지 회귀 확인
7. 테스트 데이터 삭제

## 6. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "환자별 적립금 잔액 현황 + 사용처리 기능 + 개인화면 사용내역 표시 추가"
- git push

## 완료 후 보고
- 신규 모델/화면 파일
- 검증 7단계 결과
- 화면 스크린샷(가능하면)
- git 커밋 해시