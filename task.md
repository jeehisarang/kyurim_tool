# 작업 지시서 — 본프로그램 바로등록 신청 영구 기록 + 조회 화면 신설

## 배경
체험신청은 별도 테이블(응답목록)에 영구 기록되어 목록 화면(/refer 관련)에서 언제든
조회 가능한 반면, MAIN 링크의 "바로 등록하기" 신청은 WorkTask(업무 알림) 하나로만
존재해 완료처리 후에는 사실상 기록이 남지 않음. 나중에 신청 이력을 되짚어볼 수 없는
구조적 공백이므로, 체험신청과 동일하게 영구 기록 + 조회 화면을 만든다.

## 1. 데이터 모델
- 신규 모델(가칭 MainDirectRegistrationRequest) 추가:
  - 신청자 이름, 연락처
  - 추천인(소개자) patientId/이름
  - 프로그램 종류(1개월/3개월 등 신청 시점에 알 수 있는 정보가 있다면 포함,
    없다면 생략 가능 — 상담 단계이므로 미확정일 수 있음)
  - 신청 일시
  - 처리상태(예: 대기/연락완료/등록전환/미전환) — 체험신청 목록의 "등록완료" 배지와
    유사한 개념으로, 담당자가 수기로 상태를 갱신할 수 있게
  - 연결된 WorkTask id (기존 알림 로직과 연결 유지)
- requestMainDirectRegistrationCallback 함수 수정: 기존 WorkTask 생성 로직은 그대로
  유지하되, 이 신규 테이블에도 함께 기록 저장

## 2. 조회 화면
- 기존 "체험 신청 응답" 목록 화면과 유사한 디자인/패턴으로 신규 화면 추가
  (예: /main-registration-requests 또는 기존 화면에 탭으로 추가)
- 목록에 이름/연락처/추천인/신청일시/처리상태 표시
- 클릭 시 상세 확인 가능하게 (체험신청 목록의 "클릭하면 원본 응답 확인" 패턴 참고)
- 처리상태를 담당자가 클릭해서 수동 변경 가능하게 (등록전환/미전환 등)

## 3. 검증 (테스트 전용 데이터로)
1. 테스트로 MAIN "바로 등록하기" 신청 제출
2. 신규 테이블에 정상 저장 확인
3. 신규 조회 화면에서 목록에 뜨는지 확인
4. 기존 WorkTask 알림(오늘 할일)도 여전히 정상 생성되는지 회귀 확인
5. 처리상태 수동 변경 기능 정상 동작 확인
6. 테스트 데이터 삭제

## Git 커밋 및 푸시
- git add . / commit -m "본프로그램 바로등록 신청 영구기록 테이블 및 조회화면 신설" / push

## 완료 후 보고
- 신규 모델/화면 파일
- 검증 결과
- 화면 스크린샷 (가능하면)