# 작업지시서 — 업무(WORK) 등록 원자성 수정 + 고아 레코드 정리

## 배경
지난 조사(증상 B)에서 확인된 원인 그대로 수정한다:
`createWorkTask()`가 `TodoTask.create()`와 `WorkTask.create()`를 트랜잭션 없이
순차 실행해, 두 번째가 실패하면 짝 없는 고아 TodoTask가 영구히 남는 구조.
이 고아 레코드는 체크/삭제 시 `WorkTask.findUniqueOrThrow()`에서 예외가 나서
API가 500을 반환하는데, `/home`의 handleCheck가 응답 상태를 확인 안 하고
무조건 새로고침해서 "체크해도 안 되고 삭제해도 안 되는" 것처럼 보였음.

## 수정 항목

### 1) createWorkTask 트랜잭션화 (핵심 수정)
- `TodoTask.create()` + `WorkTask.create()`를 `prisma.$transaction([...])`으로
  묶어 원자성 확보 — 하나라도 실패하면 둘 다 롤백되도록
- 기존 세 가지 등록 방식(특정담당자 지정/전체공통/개인메모) 전부 동일하게 적용

### 2) completeWorkTask / deleteWorkTask 방어 처리
- `WorkTask.findUniqueOrThrow()`에서 못 찾을 경우 예외를 던지는 대신:
  - 완료 처리 요청이면 고아 TodoTask를 그냥 완료 처리
  - 삭제 요청이면 고아 TodoTask를 그냥 삭제
- 이 방어 로직이 들어가면 기존 고아 레코드(id 844, 845)도 화면에서
  정상적으로 체크/삭제 가능해짐 — 별도 DB 직접 조작 없이 UI로 정리되게

### 3) /home의 handleCheck 에러 처리 추가
- `/todo` 페이지와 동일하게 응답 `res.ok` 확인 후, 실패 시 에러 문구 표시
- 실패를 조용히 삼키고 새로고침만 하는 현재 동작 제거

## 검증 요구사항
- 트랜잭션 적용 후: 의도적으로 두 번째 create를 실패시키는 테스트(예: WorkTask
  쪽에 잘못된 FK 주입 등)를 만들어, TodoTask도 함께 롤백되어 고아가 안 생기는지
  확인
- 세 가지 등록 방식(특정담당자/전체공통/개인메모) 각각 정상 등록 → /home,
  /todo 양쪽에서 정상 노출 확인 (회귀 없음 확인)
- 기존 고아 레코드(id 844, 845)를 실제 화면에서 체크 또는 삭제 시도해서
  정상적으로 처리되는지 확인 — 이걸로 실제 정리까지 완료
- `/home` handleCheck 실패 시 에러 문구가 실제로 뜨는지 (강제로 실패 케이스
  재현해서) 확인
- npx tsc --noEmit / npm run build 통과

## 주의
- 테스트 등록은 테스트 환자(ID 426) 또는 명백한 테스트 데이터로만
- 기존 고아 레코드(844, 845)는 코드 수정 후 화면(UI)을 통해 정리 — DB 직접
  삭제하지 말 것 (이 자체가 방어 로직이 잘 동작하는지 확인하는 검증 겸함)

## 완료 후 보고
- 트랜잭션 적용 diff 요약
- 강제 실패 재현 테스트 결과
- 고아 레코드(844, 845) 정리 결과
- 빌드 통과 여부# 작업지시서 — 업무(WORK) 등록 원자성 수정 + 고아 레코드 정리

## 배경
지난 조사(증상 B)에서 확인된 원인 그대로 수정한다:
`createWorkTask()`가 `TodoTask.create()`와 `WorkTask.create()`를 트랜잭션 없이
순차 실행해, 두 번째가 실패하면 짝 없는 고아 TodoTask가 영구히 남는 구조.
이 고아 레코드는 체크/삭제 시 `WorkTask.findUniqueOrThrow()`에서 예외가 나서
API가 500을 반환하는데, `/home`의 handleCheck가 응답 상태를 확인 안 하고
무조건 새로고침해서 "체크해도 안 되고 삭제해도 안 되는" 것처럼 보였음.

## 수정 항목

### 1) createWorkTask 트랜잭션화 (핵심 수정)
- `TodoTask.create()` + `WorkTask.create()`를 `prisma.$transaction([...])`으로
  묶어 원자성 확보 — 하나라도 실패하면 둘 다 롤백되도록
- 기존 세 가지 등록 방식(특정담당자 지정/전체공통/개인메모) 전부 동일하게 적용

### 2) completeWorkTask / deleteWorkTask 방어 처리
- `WorkTask.findUniqueOrThrow()`에서 못 찾을 경우 예외를 던지는 대신:
  - 완료 처리 요청이면 고아 TodoTask를 그냥 완료 처리
  - 삭제 요청이면 고아 TodoTask를 그냥 삭제
- 이 방어 로직이 들어가면 기존 고아 레코드(id 844, 845)도 화면에서
  정상적으로 체크/삭제 가능해짐 — 별도 DB 직접 조작 없이 UI로 정리되게

### 3) /home의 handleCheck 에러 처리 추가
- `/todo` 페이지와 동일하게 응답 `res.ok` 확인 후, 실패 시 에러 문구 표시
- 실패를 조용히 삼키고 새로고침만 하는 현재 동작 제거

## 검증 요구사항
- 트랜잭션 적용 후: 의도적으로 두 번째 create를 실패시키는 테스트(예: WorkTask
  쪽에 잘못된 FK 주입 등)를 만들어, TodoTask도 함께 롤백되어 고아가 안 생기는지
  확인
- 세 가지 등록 방식(특정담당자/전체공통/개인메모) 각각 정상 등록 → /home,
  /todo 양쪽에서 정상 노출 확인 (회귀 없음 확인)
- 기존 고아 레코드(id 844, 845)를 실제 화면에서 체크 또는 삭제 시도해서
  정상적으로 처리되는지 확인 — 이걸로 실제 정리까지 완료
- `/home` handleCheck 실패 시 에러 문구가 실제로 뜨는지 (강제로 실패 케이스
  재현해서) 확인
- npx tsc --noEmit / npm run build 통과

## 주의
- 테스트 등록은 테스트 환자(ID 426) 또는 명백한 테스트 데이터로만
- 기존 고아 레코드(844, 845)는 코드 수정 후 화면(UI)을 통해 정리 — DB 직접
  삭제하지 말 것 (이 자체가 방어 로직이 잘 동작하는지 확인하는 검증 겸함)

## 완료 후 보고
- 트랜잭션 적용 diff 요약
- 강제 실패 재현 테스트 결과
- 고아 레코드(844, 845) 정리 결과
- 빌드 통과 여부