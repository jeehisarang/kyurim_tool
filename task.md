# 작업 지시서 — AI생성 화면 정리: 웰컴/만남톡 제거 + "자유톡"(범용 문자생성) 신설

## 배경
실사용 중 확인된 개선사항. 규림툴의 핵심기능은 "환자별 맞춤 CRM 문자생성"이므로,
앞으로 관련 기능은 별도 메뉴/카테고리를 새로 만들기보다 기존 "AI 생성" 화면 안에서
확장하는 방향으로 정리한다 (향후 유사 기능 추가 시에도 이 원칙 유지).

---

## 1. 설계 원칙 (참고용, 향후 작업에도 적용)
- 환자 대상 문자/메시지 생성과 관련된 신규 기능은 원칙적으로 AI생성 화면 내 섹션으로
  추가한다. 별도 사이드바 메뉴를 새로 만들지 않는다.

## 2. 제거 — 웰컴 메시지 / 만남톡 섹션
- AI생성 > 톡생성 화면에서 "웰컴 메시지" 섹션과 "만남톡" 섹션 제거
- 실사용 확인 결과 두 항목 모두 사용 안 함 (웰컴톡은 한차트에서 별도 발송,
  만남톡도 미사용 확정)
- 관련 데이터/로직(발송확인 상태 등)은 DB에서 삭제하지 말고, UI에서만 숨김
  처리 (레거시 데이터 보존 원칙 — 혹시 과거 기록 조회 필요시 대비)

## 3. 신설 — "자유톡" (범용 AI 문자생성)
- 기존 검사톡/2일톡/7일톡/3회톡 섹션 아래(또는 별도 탭)에 "자유톡" 섹션 추가
- 구성:
  - 담당자가 자유 텍스트로 "생성하고 싶은 메시지의 목적/내용"을 입력하는 입력창
    (예: "다음주 명절 인사 문자 써줘", "3개월 진행 축하하고 4개월차 연장 제안하는 톡")
  - "생성" 버튼 클릭 시, 현재 선택된 환자(화면 상단 "선택된 환자" 영역과 연동)의
    메모/이력을 참고하여 AI가 맞춤 문구 생성
  - 생성된 문구는 기존 톡들과 동일하게 텍스트박스에 표시, 수정 가능, 복사 버튼 제공
  - "링크 포함하기" 옵션(프로그램티칭/이벤트/검사결과/추천링크)도 기존처럼 동일하게
    적용 가능하도록 재사용
- AI 프롬프트 설계 시 기존 원칙 준수:
  - 비문 방지 자체검토(self-check) 지시문 포함
  - 정확성이 필수인 정보(수치, 날짜 등)는 AI 생성이 아니라 코드에서 값을 채워
    프롬프트에 전달하는 방식으로 처리
  - 카카오톡 대화창 기준 과도하게 길지 않도록 분량 가이드 포함 (기존 킬팻캡슐
    톡 분량 축소 작업과 동일한 방향)

## 4. 검증 (테스트 전용 환자로)
1. 웰컴/만남톡 섹션이 화면에서 사라졌는지 확인, 다른 톡(검사/2일/7일/3회) 섹션은
   그대로 정상 노출되는지 확인
2. 테스트 환자 선택 → 자유톡에 임의 목적 텍스트 입력 → 생성 클릭 → 문구 정상
   생성되는지 확인
3. 생성된 문구 텍스트박스 수정 가능 여부, 복사 버튼 동작 확인
4. "링크 포함하기" 옵션 체크 시 자유톡 문구에도 정상 반영되는지 확인
5. 기존 검사톡/2일톡/7일톡/3회톡 기능이 이번 변경으로 영향받지 않았는지 회귀 확인
6. 기존 발송이력 데이터(웰컴/만남톡 관련) 조회는 그대로 가능한지(삭제 아닌 숨김
   처리 확인) — DB 직접 조회로 확인

## 5. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "AI생성 화면: 웰컴/만남톡 섹션 숨김 처리 + 자유톡(범용 문자생성) 신설"
- git push

## 완료 후 보고
- 변경/신규 파일
- 자유톡 실제 생성 예시 문구 1~2개
- 검증 6단계 결과
- git 커밋 해시, push 성공 여부