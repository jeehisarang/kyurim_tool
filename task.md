# 작업 지시서 — AI 문구생성 재구조화 + 네이버예약 클로징 완전 제거, 규림툴 링크 소개 클로징으로 전환

## 배경 (누적)
1단계 감사 결과 승인됨. COMMON_SYSTEM_PROMPT에 남아있던 카테고리 전용 요소
(예약유도 문단예시, 2일차/7일차/3회차 카테고리명 노출) 제거 및 재구조화 진행.
추가로, 규림툴 자체 링크 기능(이벤트/프로그램티칭/검사결과/추천링크)이 충분히
갖춰졌으므로 네이버예약 링크 클로징(BOOKING_LINK_CLOSING_INSTRUCTION)은
전체 폐기하고, 그 자리를 "포함된 규림툴 링크를 자연스럽게 소개하는 클로징"으로
대체한다.

---

## 1. COMMON_SYSTEM_PROMPT 재구조화 (1단계 감사 결과 반영)
- "인사→본문→마무리(예약유도)" 문단구조 예시에서 "(예약유도)" 제거,
  일반화된 형태로 수정 (예: "인사→본문→마무리")
- "상담기록이 메시지 맥락(2일차/7일차/3회차)과 관련있다면" 문구에서 괄호 안
  카테고리명 제거, 일반화 (예: "상담기록이 이번 메시지 목적과 관련있다면")
- "치료의 중요성은 분명히 짚어주되..." 지시는 완전 제거하지 말고, "관련
  있으면 자연스럽게 언급 가능, 없으면 생략" 형태로 약화
- "4~6줄" 등 분량 숫자는 COMMON에서 제거하고 각 카테고리 프롬프트에 개별 명시
  (DAY2/DAY7/THIRD_VISIT: 기존 숫자 유지, FREEFORM: 400자 유지, HAPPY_TALK: 별도 확인)

## 2. 네이버예약 클로징 완전 제거
- BOOKING_LINK_CLOSING_INSTRUCTION 삭제 (DAY2/DAY7/THIRD_VISIT/HAPPY_TALK 등
  이를 참조하던 모든 곳에서 제거)
- BOOKING_LINK 관련 상수/설정값도 이번 기회에 정리 (더는 어디서도 쓰지 않는다면
  완전 제거, 혹시 다른 곳에서 참조 중이면 그 부분도 같이 확인 후 정리)

## 3. 신규 클로징 로직 — "포함된 규림툴 링크 자연스럽게 소개"
- 현재 "링크 포함하기" 옵션(프로그램티칭/이벤트/검사결과/추천링크)에서 실제로
  체크되어 생성된 링크가 있는 경우, AI 프롬프트에 "다음 링크를 자연스러운 문장으로
  소개하며 마무리에 포함시켜라"는 지시와 함께 해당 링크 URL을 전달
  - 링크 종류별로 소개 톤 예시 제공 (하드코딩 문자열이 아니라 AI가 자연스럽게
    구성하되, 아래를 참고 예시로 프롬프트에 포함):
    - 프로그램티칭: "자세한 내용은 아래에서 확인해보세요"
    - 이벤트: "이벤트 소식은 아래 링크에서 확인해보세요"
    - 검사결과: "검사 결과는 아래에서 확인 가능합니다"
    - 추천링크: "링크는 아래에서 확인해보세요" (추천링크 관련 톤은 기존 자유톡
      추천안내 예시 참고)
- **링크가 하나도 포함 안 된 경우**: 예약 유도나 특정 URL 안내 없이, 목적에 맞게
  자연스럽게 마무리 (강제 클로징 문구 없음 — 순수 목적 기반 마무리)
- 여러 링크가 동시에 포함된 경우(예: 프로그램티칭 + 추천링크 동시 체크) 처리
  방식도 확인 필요 — 각각을 자연스럽게 순서대로 소개하도록 프롬프트 구성

## 4. 검증
1. 재구조화 후 각 카테고리(FREEFORM, HAPPY_TALK, DAY2, DAY7, THIRD_VISIT)로
   링크 미포함 상태에서 생성 → 네이버예약 문구가 완전히 사라졌는지, 그리고
   "2일차/7일차/3회차" 같은 카테고리명이 결과물에 새어나오지 않는지 확인
2. 각 카테고리에서 링크 옵션(이벤트/프로그램티칭/검사결과/추천링크) 하나씩
   체크해서 생성 → 클로징이 자연스럽게 해당 링크를 소개하는 형태로 나오는지 확인
3. 링크 두 개 이상 동시 체크 시 처리 확인
4. 기존 실사용 톡(DAY2/DAY7)에 네이버예약 링크가 더는 없다는 점, 담당자들이
   혼란 없도록 확인 (기능적으로는 문제없지만, 실사용 확인 차원)
5. HAPPY_TALK: 처음 실사용 예정이므로 특히 꼼꼼히 — 다음 처방일 안내 + (필요시)
   추천링크 소개가 자연스럽게 함께 나오는 조합도 테스트

## 5. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "AI 문구생성: 프롬프트 재구조화 + 네이버예약 클로징 제거, 규림툴 링크 소개 클로징으로 전환"
- git push

## 완료 후 보고
- 재구조화 최종 프롬프트 구조 (카테고리별 최종 조립 방식)
- 네이버예약 관련 코드/상수 제거 확인
- 검증 5단계 각각의 실제 생성 예시 포함해서 보고
- git 커밋 해시