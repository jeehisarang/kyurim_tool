# 작업지시서: 킬팻캡슐 3일체험 신청 시 알림톡 자동발송 연동 (발송닷컴)

## 배경
- 킬팻캡슐 3일체험 신청 완료 시, 발송닷컴 API로 "킬팻캡슐신청본인확인" 알림톡(템플릿 코드 14361)을 자동발송
- 지금까지는 신청이 들어와도 발송닷컴에 아무 연동이 없어 수동으로 처리 중이었음 — 이번에 자동화

## 1단계: 조사 (구현 전 먼저 확인, 보고 후 승인받고 진행)

1. 킬팻캡슐 3일체험 신청 폼 제출 처리 로직 위치 확인 (API route 또는 서버 액션)
2. 신청 시 저장되는 환자 데이터 구조 확인 — 이름, 전화번호 필드명 확인 (Destination에 넣을 값)
3. 템플릿 14361의 실제 변수 스펙 확인 — 템플릿 내용상 `#{이름}` 외 추가 치환변수가 있는지 (스크린샷 기준으론 `#{이름}` 하나로 보임)
4. 발신번호(Callback), Channel_ID(@규림천안 등 정확한 값) 확인 — 원장님께 재확인 또는 발송닷컴 관리자 페이지에서 조회

## 2단계: 환경변수 등록

`.env`에 아래 추가 (절대 클라이언트 코드/커밋 로그에 노출 금지):
- BALSONG_USER_ID
- BALSONG_USER_PW
- BALSONG_CALLBACK (발신번호)
- BALSONG_CHANNEL_ID (예: @규림천안)
- BALSONG_TEMPLATE_SEQ=14361

## 3단계: 발송 API 연동 (서버 사이드 전용)

- 엔드포인트: `POST https://balsong.com/Linkage/API/`
- Content-Type: `multipart/form-data`
- 요청 파라미터:
  - UserID, UserPW (env에서)
  - Service: "ATALK"
  - Type: "Send"
  - Channel_ID: env값
  - Template_Seq: "14361"
  - Callback: env값 (발신번호)
  - Fail_To: 빈 값 (SMS 자동전환 안 함 — 확정사항)
  - Subject: 임의 제목 (예: "킬팻캡슐 신청 본인확인")
  - Main_Text: 대표내용 (템플릿 요약 텍스트, 실제발송내용 아님 — 아무 문구나 가능)
  - Destination: JSON 배열, 신청 1건당 1개 원소
    - Company: "규림한의원" (또는 임의값)
    - Name: 환자 이름
    - Phone: 환자 전화번호 (숫자만, 하이픈 제거)
    - Replace_Datas: [{"Key": "#{이름}", "Value": 환자이름}]
    - Buttons: [{"name": "신청 본인 확인", "type": "BK"}]  ← 봇키워드 타입 확정
- 반복문(for/while)으로 여러 건 개별 호출 금지 원칙 준수 — 지금은 신청 1건 = 발송 1건 구조라 문제 없지만, 혹시 동시신청 몰릴 경우 대비해 초당 3회 제한 걸리지 않도록 간단한 큐/딜레이 처리 고려 (필수는 아니고 여유 있으면 반영)

## 4단계: 발송 결과 저장 + 실패 추적

- 응답 JSON에서 `Result`, `Code`, `Job_No` 저장
  - Result: OK / ERROR
  - Job_No: 나중에 상태조회에 사용할 키
- 신규 테이블 또는 기존 신청 테이블에 컬럼 추가(ADD COLUMN 원칙): `alimtalkJobNo`, `alimtalkSendResult`, `alimtalkSentAt`
- **실패 확인 로직**: 발송 후 일정 시간 뒤(예: 5~10분 후) `Type: "Report_Detail"` + 해당 Job_No로 상태 조회 → `Status`가 "실패"면 관리자 화면에 표시
  - 폴링 방식: 기존 검사 리마인더 등에서 쓰는 주기적 재확인 패턴 재사용 가능
  - 실패 발견 시 노출 위치: "승인 대기 큐" 또는 별도 알림톡 발송현황 화면에 "⚠️ 발송실패 - [환자명] - [사유]" 형태로 표시

## 5단계: 테스트

- 실제 신청 폼으로 테스트하되, 전화번호는 **010-0000-0000**(실존하지 않는 번호, 발송닷컴 안내 방식)으로 입력해 테스트
- 테스트 후 응답 JSON(Result/Code/Job_No) 그대로 콘솔 로그로 확인 → 정상 응답 오는지 1차 확인
- 가능하면 원장님 실제 번호로 1건 테스트 발송해 카카오톡으로 실제 수신 확인 (템플릿 승인 완료 상태이므로 실발송 가능)
- 테스트 데이터는 완료 후 정리

## 주의사항
- UserID/UserPW는 서버에서만 사용, 클라이언트 번들에 절대 포함 금지
- 신청 폼 제출 → 자동발송까지 실패해도 신청 자체(DB 저장)는 반드시 성공해야 함 — 알림톡 발송 실패가 신청 접수 자체를 막으면 안 됨 (발송은 best-effort, 신청 저장이 우선)
- tsc/build 통과 확인 후, 브라우저에서 실제 신청 흐름 테스트 → 커밋/푸시