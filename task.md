# 작업지시서: 이명은(3601? — 실제 차트번호 확인 필요) 킬팻캡슐3개월 프로그램 미노출 버그 조사

## 배경
- 이명은 환자, 킬팻캡슐3개월 등록 시작일 2026-05-25로 입력 시 "치료처방" 목록(킬팻캡슐 진행중 탭)에서 사라짐
- 시작일을 2026-06-01로 바꾸면 정상적으로 다시 나타남
- 다른 조건 변화 없이 시작일 하루~며칠 차이만으로 렌더링 여부가 바뀌는 것으로 보아, 날짜 계산 로직의 특정 케이스 버그로 추정

## 조사 (구현 전 원인부터 확인, 결과 보고 후 진행)

1. 이명은 환자(차트번호 확인) 실제 DB 레코드 확인 — 시작일 5/25로 저장했을 때의 실제 값(치료처방 테이블 row 전체, PrescriptionRoundOverride 등 관련 테이블 포함)
2. "킬팻캡슐 진행중" 목록을 조회하는 쿼리/함수(예: listActiveKillCapPatients() 계열) 로직 확인
   - 시작일 기준으로 "진행중"을 판단하는 조건이 무엇인지 (예: 시작일 + 3개월 < 오늘이면 종료 처리? 또는 회차 계산 중 날짜 범위를 벗어나면 필터링?)
   - 5/25와 6/1의 차이(7일)가 어떤 경계값(월경계, 특정 회차 계산, 2주 단위 나눗셈 등)에 걸리는지 확인
3. 콘솔 에러 여부 확인 — 날짜 계산 중 예외가 발생해서 해당 환자만 조용히 필터링(try-catch로 삼켜지는 등)되는 구조인지 확인
4. 재현 테스트: 테스트 환자(ID 426)로 동일하게 5/25, 5/26, 5/27... 6/1까지 하루씩 바꿔가며 어느 날짜부터 사라지는지 정확한 경계값 특정 (dry-run, 완료 후 정리)

## 주의사항
- 라이브 환자(이명은) 데이터는 조회만 하고 수정/삭제 금지 — 재현 테스트는 반드시 테스트 환자(426)로
- 원인 파악 후 수정안은 보고 후 승인받고 진행