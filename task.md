# 작업 지시서 — 추천 이벤트 적립금 구조 개선 (1개월/3개월 차등 + 링크 승격 + 랜딩페이지 분기)

## 배경
기존 추천 이벤트(체험 5천원 / 본프로그램 소개 7만·3만)는 프로그램 기간(1개월/3개월) 구분 없이
동일 금액이 적용되어 있었고, TRIAL 등급 링크가 본프로그램 등록 후에도 그대로 체험 유도
설문지로만 연결되어 MAIN 등급 보상(7만/3만)에 도달할 기회 자체가 거의 열리지 않는 문제가
있었음. 아래 내용으로 개선.

---

## 0. 작업 전 필수 확인사항 (반드시 먼저 수행)
- git status로 원장실 PC dev.db 최신 상태 확인
- 오늘 작업은 절대 실제 환자 데이터에 자동화 스크립트를 직접 돌리지 말 것.
  검증은 반드시 테스트 전용 환자(예: 426번)로만 진행
- 실제 신청자 데이터(어제 기준 10명+)는 절대 수정/삭제 대상 아님 — 조회만

---

## 1. 데이터 모델 변경

### 1-1. ReferralLink에 등급(tier) 개념 추가
- 기존 ReferralLink 모델 확인 후, 다음 필드 추가 (기존 필드/데이터는 유지, 하위호환 필수):
  - `tier`: enum 또는 string ("TRIAL" | "MAIN"), 기본값 "TRIAL"
  - `expiresAt`: 기존 필드 있으면 재사용, 없으면 신규 추가
- 마이그레이션은 반드시 기존 컬럼 삭제 없이 ADD COLUMN 방식으로만 작성할 것
  (레거시 데이터 보존 원칙, 기존 발급된 링크는 전부 tier="TRIAL"로 기본 세팅)

### 1-2. 프로그램별 적립금 설정값 분리
- TrialCampaignSettings(또는 관련 설정 모델)에 다음 값 추가:
  - 체험 소개 적립금: 5,000원 (기존값 유지)
  - MAIN 소개 적립금 — 1개월: 소개자 35,000원 / 피소개자 15,000원
  - MAIN 소개 적립금 — 3개월: 소개자 70,000원 / 피소개자 30,000원
- 하드코딩 금지, 반드시 설정화면(/settings 등 기존 위치)에서 조정 가능하게 구현

---

## 2. 링크 승격(TRIAL → MAIN) 로직

- 환자가 본프로그램(1개월 또는 3개월) 치료처방 등록 완료 시:
  - 해당 환자의 기존 ReferralLink 1건을 찾아 tier를 "MAIN"으로 승격
  - expiresAt 갱신: 1개월 등록 시 현재로부터 +3개월(기간 미정이나 우선 3개월로 구현,
    추후 조정 가능하도록 상수로 분리), 3개월 등록 시 +6개월
  - 링크 URL/토큰 자체는 변경하지 않음 (기존 링크 그대로 유지, 필드만 갱신)
- 환자당 링크는 항상 1개만 존재 — 신규 링크를 추가 발급하지 말 것

---

## 3. 랜딩페이지 분기 (가장 중요한 부분)

- `/refer/trial/[token]` (또는 해당 공개 신청페이지) 진입 시, 토큰에 연결된 ReferralLink의
  tier를 조회:
  - **tier === "TRIAL"**: 기존 그대로 체험 유도 설문 흐름 (변경 없음)
  - **tier === "MAIN"**: 진입 시 먼저 선택 화면을 보여줄 것
    - 옵션 A: "3일 무료체험 먼저 해보기" → 클릭 시 기존 체험 신청 설문으로 이동
    - 옵션 B: "바로 등록하고 할인받기" → 친구 소개 혜택(피소개자 할인액, 프로그램별로
      15,000원/30,000원) 강조 문구 노출 → 클릭 시 본프로그램 직접 등록/상담 신청 플로우로 연결
      (기존 "본상담 희망" 업무 자동생성 로직과 연동)

---

## 4. 보상 트리거 로직 (신규/수정)

### 4-1. 체험 신청 시 (기존 로직 유지)
- 신청 시점: 소개자에게 PENDING 5,000원
- 원내 방문 후 실제 체험 참여 확정(약 수령) 시: CONFIRMED 전환
- **주의**: 체험 경유로 신청한 사람이 나중에 스스로 본프로그램 등록해도 추가 MAIN 보상 없음.
  즉 "이 신청 건이 TRIAL 경로로 시작됐는지"는 기록하되, 그 이후 본프로그램 전환 여부와
  소개자 보상은 별개 이벤트로 처리하지 않음 (기존 설계 그대로, 재확인 차원)

### 4-2. MAIN 등급 링크를 통한 직접 등록 시 (신규)
- 랜딩페이지에서 "바로 등록" 선택 후 실제 본프로그램(1개월/3개월) 등록 완료 시:
  - 소개자: 프로그램에 맞는 금액(35,000원 또는 70,000원) PENDING 적립,
    결제 완료 확인 시 CONFIRMED
  - 피소개자: 등록 시점에 즉시 할인 적용(15,000원 또는 30,000원)
- 이 이벤트는 체험 신청 이벤트와 완전히 독립적으로 카운트 (같은 소개자가 동시에
  체험소개 건과 MAIN소개 건을 각각 보유 가능)

### 4-3. 2차/3차 확산
- 별도 처리 불필요 — 각 환자는 자기 링크로 발생한 이벤트에 대해서만 보상받음
  (체인 조회 로직 만들지 말 것, 현재 설계가 이미 이 원칙에 부합하므로 회귀 테스트만 확인)

---

## 5. 검증 (테스트 전용 환자로만 진행)

1. 테스트 환자 A(426번 등)로 체험 신청 → 5,000원 PENDING 확인
2. A를 원내 방문 처리(체험 확정) → CONFIRMED 전환 확인
3. A를 본프로그램 3개월로 등록 → 
   a. A의 기존 링크가 tier="MAIN"으로 승격, expiresAt이 +6개월로 갱신되는지 확인
   b. A의 링크(동일 URL)로 랜딩페이지 접속 시 "체험 vs 바로등록" 분기 화면이 뜨는지 확인
4. 분기 화면에서 "바로 등록" 선택 → 테스트 환자 B로 3개월 등록 진행
   a. A에게 70,000원 PENDING 적립되는지 확인
   b. B에게 30,000원 할인이 정확히 반영되는지 확인
5. 1개월 프로그램으로도 동일 시나리오 반복해 35,000원/15,000원 정확히 적용되는지 확인
6. 기존 TRIAL 등급 링크(승격 안 된 환자)는 기존 체험 유도 화면이 그대로 나오는지 회귀 확인

---

## 6. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "추천이벤트: 1개월/3개월 적립금 차등화 + 링크 승격(MAIN) + 랜딩페이지 분기"
- git push

## 완료 후 보고
- 변경된 스키마/마이그레이션 파일명
- 검증 5단계 각각의 실제 결과(스크린샷 또는 로그)
- git 커밋 해시, push 성공 여부
- 실데이터 영향 없음 확인 (기존 신청자 10명+ 데이터 변동 없는지 재확인)