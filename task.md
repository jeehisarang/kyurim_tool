# 작업 지시서 — DAY2 few-shot 예시의 낡은 링크 언급 제거 (검사결과 오언급 버그)

## 배경
DAY2 톡 생성 시, 실제로는 "프로그램티칭 + 이벤트 + 추천링크"만 체크했는데, 결과
문구에 "검사 결과와 추천 프로그램을 아래 링크에서 확인해보세요"라는, 실제
체크하지 않은 "검사결과"를 언급하는 문장이 나옴. 예전에 검사톡+프로그램 링크를
함께 보내려던 상황을 만들 때 넣어둔 few-shot 예시 문구가 그대로 남아있어서,
지금 상황(검사결과 미포함)에도 그 표현이 그대로 재현되는 것으로 추정됨.

## 1. 원인 확인
- DAY2/DAY7/THIRD_VISIT few-shot 예시(SYSTEM_PROMPT 내 예시 문장들) 전수 확인
- "검사 결과와 추천 프로그램을 아래 링크에서 확인해보세요" 또는 유사한 고정
  문구가 예시에 하드코딩되어 있는지 확인
- 있다면, 이 예시가 실제 includedLinks 데이터와 무관하게 AI에게 "이런 식으로
  링크 종류를 나열해서 말해도 된다"는 잘못된 패턴을 학습시키고 있는 것이 원인

## 2. 수정 방향
- few-shot 예시에서 특정 링크 조합(검사결과+프로그램 등)을 구체적으로 언급하는
  문구는 실제 링크 종류를 특정하지 않는 형태로 일반화
  (예: "김우석님의 OOO를 아래 링크에서 확인해보세요" 대신, 실제 전달된
  includedLinks 배열에 있는 링크 종류만 정확히 언급하도록 지시)
- 근본적으로, AI가 "체크되지 않은 링크 종류를 절대 언급하지 말 것"이라는
  명시적 금지 지시문을 프롬프트에 추가 (예: "아래 목록에 없는 링크 종류
  — 검사결과, 이벤트, 추천링크, 프로그램티칭 등 — 는 절대 언급하지 마라.
  실제 전달된 링크만 정확히 소개하라")
- few-shot 예시들도 실제 체크박스 조합과 1:1 대응하도록 재작성 (예시 자체가
  실제 동작 가능한 조합을 반영하게)

## 3. 검증
1. 이번 버그 재현 조건 그대로(프로그램티칭+이벤트+추천링크 체크, 검사결과 미체크)
   재생성 → "검사 결과" 언급이 완전히 사라지고 실제 체크된 3종만 정확히
   언급되는지 확인
2. 다른 조합(검사결과만 단독, 4종 전부 등) 각각 테스트해서 체크 안 한 종류가
   전혀 언급되지 않는지 전수 확인
3. 링크 미포함(0종) 상태에서도 정상 생성되는지 회귀 확인
4. 지난번 수정한 "링크 반드시 소개" 리마인더(LINK_REMINDER)와 이번 "체크 안 한
   링크는 언급 금지" 지시가 서로 충돌 없이 공존하는지 확인 (실제로 포함된
   것만 정확히, 빠짐없이 소개)

## 4. Git 커밋 및 푸시
- npm run build 통과 확인
- git add . / commit -m "AI 문구생성: few-shot 예시의 낡은 링크조합 언급 제거, 미체크 링크 오언급 방지" / push

## 완료 후 보고
- 원인이 된 few-shot 문구 정확히 인용
- 수정 전/후 비교 (이번 버그 재현 조건 기준)
- 다른 조합 테스트 결과
- git 커밋 해시