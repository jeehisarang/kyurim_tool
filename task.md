# 작업 지시서 — ① 전체 목록 화면 공통 검색기능 + ② 카톡연결 상태 관리

## 배경
실사용 중 발견된 두 가지 보완점:
1. 킬팻캡슐 3일체험 등록이 늘면서 내원체크/검사/오늘의 할일 목록에서 환자를 찾기 어려움
2. 내원톡 발송 시 카카오채널 대화창 미형성 사례를 직원들이 놓치는 경우 발생 —
   예전 구글시트의 "웰컴톡 대화창생성 체크" 항목이 현재 시스템에 없음

---

## 1. 전체 목록 화면 공통 검색기능

### 대상 화면
- 내원체크 (`/checkin` 등, 기존 검색 UI가 이미 있는 화면 — 이 패턴을 기준으로 재사용)
- 검사 목록 (`/examinations`)
- 오늘의 할일 (`/todo`, `/home`)
- 치료처방 목록 (있다면 동일 적용)

### 구현
- 기존 내원체크 화면의 "차트번호 또는 이름" 검색창 UI/동작을 그대로 각 화면에 이식
- 검색 시 해당 화면의 기존 필터(날짜범위, 카테고리 등)와 함께 동작하도록 구현
  (검색어 입력 시 기존 필터 조건 유지한 채로 이름/차트번호만 추가로 좁혀지는 방식)
- 각 화면이 이미 서버사이드 페이지네이션/필터링 구조인지 클라이언트 필터링 구조인지 먼저
  확인 후, 기존 방식에 맞춰 구현 (구조 통일 목적으로 무리하게 리팩터링하지 말 것)

---

## 2. 카톡연결(채널 대화창 형성) 상태 관리

### 데이터 모델
- Patient(환자) 단위로 카톡연결 상태 필드 추가: `kakaoChannelConnected` (boolean, 기본값 false)
- 기존 환자 데이터는 마이그레이션 시 전부 false(미연결)로 시작 (ADD COLUMN, nullable 또는
  기본값 지정, 레거시 데이터 무손실 원칙 준수)

### 리셋 로직 (중요 — 트리거 시점 명확히 구분)
- **내원체크 "신규 체크 등록" 액션 시에만** 다음 로직 적용:
  - 진료구분이 "초진" 또는 "재초진"으로 체크되는 경우 → 해당 환자의
    `kakaoChannelConnected`를 무조건 false로 리셋
  - 진료구분이 "재진"으로 체크되는 경우 → 기존 상태 그대로 유지 (건드리지 않음)
- **기존 체크 건 "수정" 액션에서는 이 리셋 로직을 절대 발동시키지 않음**
  (진료구분을 초진→재진 등으로 정정하더라도 kakaoChannelConnected 값은 변경하지 않음)
- 즉 리셋은 "신규 체크 생성" API/함수 경로에서만 실행되도록 명확히 분리해서 구현할 것

### UI
- 내원체크 목록에서 예약여부 배지와 동일한 스타일로 "카톡연결" / "카톡미연결" 배지 추가
- 배지 클릭 시 미연결→연결로 토글 (연결→미연결로 직원이 되돌리는 것도 허용할지는
  일단 허용해서 구현, 추후 필요시 제한)
- 체크한 사람(담당자) 기록처럼, 카톡연결 처리한 사람/시각도 함께 기록해두면 추후
  "누가 놓쳤는지" 확인 가능 (선택사항, 기존 작업이력 로직 있으면 재사용)

## 3. 검증 (테스트 전용 환자로)
1. 테스트 환자 신규 초진 체크 등록 → kakaoChannelConnected=false 확인
2. 배지 클릭해서 연결로 전환 → true 확인
3. 같은 환자 재진으로 다음 체크 등록 → 상태가 true 그대로 유지되는지 확인
4. 같은 환자 재초진으로 체크 등록 → false로 리셋되는지 확인
5. 재진 체크 건을 "수정"으로 초진으로 정정 → kakaoChannelConnected 값이 변하지 않는지 확인
6. 검색기능: 내원체크/검사/오늘의 할일 각 화면에서 이름/차트번호로 검색해 정상 필터링되는지 확인
7. 기존 환자 데이터(실사용 데이터) 조회 시 새 필드로 인한 에러 없는지 회귀 확인

## 4. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "전체 화면 공통 검색기능 추가 + 카톡연결 상태 관리(초진/재초진 리셋) 추가"
- git push

## 완료 후 보고
- 변경된 파일/마이그레이션명
- 검증 7단계 각각의 결과
- git 커밋 해시, push 성공 여부
- 실데이터(환자/내원기록) 영향 없음 확인