# 작업지시서: 통계 대시보드 개선 (2026-08-01)

## 배경
- 7월 마감, 8월 시작 시점에서 실사용 기반 통계 대시보드 개선 요청
- 이번 범위: 확정된 지표 개선 4건 + 조사 2건. 전환퍼널 지표(3일체험 전환율, 검사후처방율, 상담버튼클릭률)는 이번 범위 제외, 별도 세션에서 논의 예정

## 1단계: 조사 (구현 전 먼저 확인, 결과 보고 후 승인받고 진행)

1. **초진/재초진 지표 현재 상태 확인**
   - "7일이내 내원율", "3회이상 재방문율" 관련 기존 로직(메모리 기준 이미 구현되어 있다고 알려짐)이 현재 대시보드 화면에 실제로 렌더링되고 있는지 확인
   - 렌더링되고 있다면 어느 위치인지 스크린샷 첨부해서 보고
   - 안 보인다면 코드가 삭제됐는지, 조건에 걸려 숨겨져 있는지 원인 파악

2. **통계 대시보드 성능 이슈 진단**
   - 대시보드 클릭 시 딜레이 발생 원인 확인 — 매 요청마다 실시간 계산(DB 풀스캔/집계쿼리)하는 구조인지 확인
   - 캐싱 여부, 쿼리 실행시간 측정(로그 또는 console.time)
   - 원인 확인 후 개선방안 제안 (예: 일 1회 배치로 사전계산 후 저장, 또는 쿼리 최적화, 또는 캐시 레이어 추가) — 구체적 수정은 별도 승인 후 진행

## 2단계: 월별 누적환자수 (콤보 차트)

- 기존 "누적환자수"(전체 총합 단일 숫자) → 월별 콤보 차트로 교체
  - 막대: 월별 신규환자수 (그 달에 처음 내원한 환자 수)
  - 꺾은선: 월말 기준 누적 총환자수 (계속 증가하는 추이)
  - X축: 월 (예: 2026-01 ~ 2026-08, 데이터 있는 범위부터)
- 기존 "인당내원수" 등 월경계 오차 최소화 로직과 동일한 기준으로 월 구분 처리 (KST 기준 월 경계)

## 3단계: 예약율 통합 + 기간 선택 UI

- "오늘예약율"과 "최근7일 평균예약율" 2개 카테고리 → 1개 "예약율" 카테고리로 통합
- 인당내원수도 동일한 기간선택 컴포넌트 재사용
- 공용 기간선택 드롭다운 컴포넌트 신설: 옵션 [최근 7일 / 최근 30일 / 이번달]
  - 기본값: 최근 7일
  - 이 컴포넌트를 예약율, 인당내원수 두 카드 모두에 적용 (드롭다운 각각 독립적으로 선택 가능하게 — 예약율은 30일, 인당내원수는 7일 이렇게 따로 볼 수 있어야 함)

## 4단계: 링크발송 후 클릭률 (신규, 간단 지표)

- 기존 페이지뷰 로깅(`/s/[token]`, `/p/[token]` 하루 1회 중복제거) 데이터 활용
- 지표: "알림톡/링크 발송 건수 대비, 실제 클릭(페이지뷰 발생)한 환자 비율"
- 계산 범위: 위에서 만든 기간선택 드롭다운과 동일한 기간 적용 (7일/30일/이번달)
- 별도 카드 1개로 추가 (기존 지표 카드들과 같은 스타일)
- 주의: 발송 로그와 페이지뷰 로그를 환자ID 또는 토큰 기준으로 매칭 — 매칭 기준 확인 후 구현 (조사 단계에서 발송 이력 테이블과 페이지뷰 테이블 연결고리 확인 필요하면 1단계에 포함해서 같이 보고)

## 이번 범위 제외 (별도 세션 예정)
- 킬팻3일체험 → 본프로그램 전환율
- 검사 후 치료처방 등록 비율
- 상담하기 버튼 클릭 비율
→ 사유: 데이터 연결구조/이벤트 로깅 여부부터 조사 필요, 실사용 데이터 더 쌓인 후 재논의

## 주의사항
- 스키마 변경 시 ADD COLUMN만 (파괴적 마이그레이션 금지)
- 기존 지표(재방문율 등) 로직에 영향 없도록 별도 컴포넌트/쿼리로 분리
- 시각적 요소(차트, 드롭다운) 완성 후 브라우저에서 실제 렌더링 확인 → 그 다음 커밋/푸시
- tsc --noEmit, npm run build 통과 확인