# 작업 지시서 — 실시간 활동피드: 본프로그램 바로등록 문의 강조표시 + 테스트 로그 잔존 확인

## 배경
실시간 활동피드에서 "본프로그램 바로등록 문의" 이벤트가 다른 일반 알림(톡 발송
확인 등)과 시각적으로 구분되지 않아 놓치기 쉬움. 이 이벤트는 실제 매출로 이어질
수 있는 중요 이벤트이므로 강조 표시 필요. 또한 지난 검증 테스트 중 생성된
"[TESTMARKER-notif]" 항목이 한글 깨짐 상태로 활동피드에 남아있는 것이 화면상
확인되어, 이 잔존 여부도 함께 확인 필요.

## 1. 강조표시 추가
- 실시간 활동피드에서 requestMainDirectRegistrationCallback으로 생성되는
  "본프로그램 바로등록 문의" 관련 이벤트 항목에 시각적 강조 추가
- 기존 "오늘 할 일" 목록에서 쓰이는 🔴 빨간 원 마커와 동일한 패턴/색상으로 통일
- 다른 일반 알림(톡 발송 확인 등)은 강조 없이 기존 스타일 그대로 유지

## 2. 테스트 데이터 로그 잔존 확인
- 실시간 활동피드에 "[TESTMARKER-notif]"로 시작하는 항목이 한글 깨짐 상태로
  남아있는 것이 화면에서 확인됨. 이번에 새로 만든 MainDirectRegistrationRequest
  테이블과는 별개로, 활동피드(ActivityFeed 등 로그 전용 테이블/모델)가 따로 존재해
  거기에는 테스트 데이터가 삭제 안 되고 남아있을 가능성 확인
- 실제로 테스트 데이터 흔적인지 확인 후, 맞다면 삭제
- 앞으로 검증 시 활동피드 로그도 정리 대상에 포함하도록 체크리스트에 반영

## 3. 검증
- 테스트 계정으로 본프로그램 바로등록 신청 → 실시간 활동피드에 빨간 강조로
  노출되는지 확인
- 위 TESTMARKER 잔존 항목이 실제로 삭제됐는지 확인
- 일반 알림(톡 발송 등)은 기존처럼 강조 없이 노출되는지 회귀 확인
- 테스트로 생성한 데이터 전부 삭제 확인

## 4. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "실시간활동피드: 본프로그램 바로등록 문의 강조표시 추가 + 테스트로그 정리"
- git push

## 완료 후 보고
- 강조표시 구현 방식 및 확인 결과
- TESTMARKER 잔존 원인 (활동피드 테이블 삭제 누락 여부, 원인 구분)
- 검증 결과
- git 커밋 해시, push 성공 여부