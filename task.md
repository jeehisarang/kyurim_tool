# 작업 지시서 — 추천현황 화면 개선(본인 이름 표시, 추천코드 노출 통일) + "바로 등록하기" 신청 알림 경로 확인

## 배경
오늘 실사용 확인 중 발견된 세 가지:
1. "내 추천 현황"(/refer/my/[token]) 화면에 본인 이름이 없어 개인화가 안 됨
2. MAIN 등급 랜딩분기 화면(MainTierLandingChoice) 이후 "바로 등록하기" 신청폼에서는
   추천코드가 노출되는데, 기존 TRIAL 체험유도 화면과 비교 시 코드 노출 방식이 일관되지
   않은 부분 확인 필요 (분기 화면 자체에는 코드 표시가 없었음)
3. MAIN 링크로 "바로 등록하기" 신청(이름/연락처만 입력하는 상담신청) 시, 이 신청건이
   현재 시스템 어디에서 확인 가능한지 아직 검증 안 됨 — 직원이 놓칠 위험 있는지 확인 필요

---

## 1. 내 추천 현황 화면 — 본인 이름 표시
- `/refer/my/[token]` 화면 상단에 환자 이름 표시 추가
  (예: "OO님의 추천 현황" 또는 상단에 이름 배지)
- 해당 토큰으로 연결된 Patient 정보 조회해서 이름만 노출 (연락처 등 민감정보는 노출 금지)

## 2. 추천코드 노출 일관성 확인 및 통일
- 기존 TRIAL 체험유도 화면: "친구의 추천으로 오셨네요! 추천코드: XXXX" 형태로 코드 노출
- MAIN 분기 화면(MainTierLandingChoice) 및 그 이후 "3일 체험" / "바로 등록" 각 하위 화면
  전부에서 동일하게 추천코드가 노출되는지 확인
- 노출 안 되는 화면이 있다면 동일한 문구/스타일로 통일 추가
  (코드는 비식별 토큰이라 노출해도 개인정보 문제 없음 — 유지 원칙으로 확정)

## 3. "바로 등록하기" 신청 알림 경로 확인 (중요 — 최우선 확인)
- MAIN 링크 → "바로 등록하고 할인받기" 선택 → 이름/연락처 입력 후 "상담 신청하기" 클릭 시:
  - 이 신청 데이터가 저장되는 모델/테이블 확인 (기존 ExitSurveyResponse, TrialApplication과
    다른 신규 흐름인지, 아니면 기존 모델을 재사용하는지 코드로 확인)
  - 저장 후 다음 중 어디에 반영되는지 실제로 확인:
    - "오늘 할 일" 목록에 업무가 자동 생성되는지
    - 실시간 활동피드에 반영되는지
    - 별도 목록/설정 화면이 있다면 그 화면에서 조회 가능한지
  - **만약 이 신청이 어디에도 노출되지 않고 DB에만 저장되고 끝난다면, 이건 반드시 수정
    필요** — 최소한 "오늘 할 일"에 "MAIN 소개 상담 신청 — [이름]/[연락처] 확인 필요" 형태로
    업무가 자동 생성되도록 구현할 것 (기존 체험 신청 시 업무 자동생성 로직과 동일 패턴 재사용)

## 4. 검증 (테스트 전용 환자/데이터로)
1. 테스트 환자 링크로 `/refer/my/[token]` 접속 → 이름 정상 노출 확인
2. TRIAL/MAIN 각 경로별로 추천코드 노출 여부 전수 확인, 통일 여부 확인
3. MAIN 링크 → "바로 등록하기" → 테스트 이름/연락처로 신청 제출
4. 제출 직후 "오늘 할 일" 목록에 해당 신청 업무가 뜨는지 확인 (없었다면 3번 작업으로
   추가 구현 후 재확인)
5. 테스트로 생성된 데이터 전부 삭제 확인

## 5. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "추천현황 화면 개선(이름표시/코드노출 통일) + MAIN 직접등록 신청 알림 경로 보완"
- git push

## 완료 후 보고
- 각 항목별 변경 파일
- 3번 항목: "바로 등록하기" 신청건이 원래 어디에도 안 보였는지, 아니면 이미 어딘가 있었는데
  원장님이 못 찾으신 것뿐인지 — 원인 명확히 구분해서 보고
- 검증 5단계 결과
- git 커밋 해시, push 성공 여부