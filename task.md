# 작업 지시서 — 킬팻캡슐 배지 색상 구분 + 세부 필터(2단) 추가

## 배경
치료처방 목록에서 "킬팻캡슐" 필터 하나로 3일체험/1개월/3개월이 전부 묶여 있어,
목록을 눈으로 훑을 때 배지 색이 동일해 구분이 안 되고, 필터도 세분화가 안 되어
있어 원하는 종류만 골라보기 어려움.

## 1. 배지 색상 구분
- 진행중인 프로그램 배지("킬팻캡슐·3일체험", "킬팻캡슐·3개월" 등) 색상을
  프로그램 종류별로 다르게 표시:
  - 킬팻캡슐 3일체험: 기존 색 유지(또는 연한 톤)
  - 킬팻캡슐 1개월/3개월(본프로그램): 확실히 구분되는 색(브랜드 골드/적갈색
    계열 등, 기존 규림한의원 브랜드 팔레트 참고 — 단, 소프트웨어 UI에는
    청자색/한지/주홍 팔레트가 이미 쓰이고 있으니 그 팔레트 내에서 구분)
- 다른 카테고리(탕약/환/첩약건보)의 기존 배지 색상 체계와 충돌하지 않게 확인

## 2. 세부 필터 추가 (2단 구조)
- 기존 1단 필터(전체보기/탕약/환/킬팻캡슐/첩약건보)는 그대로 유지
- "킬팻캡슐" 필터 선택 시, 그 아래 세부 필터 버튼 한 줄 추가로 노출:
  - 킬팻캡슐 전체 / 3일체험 / 1개월 / 3개월
- 세부 필터 선택 시 목록이 해당 종류만 필터링되도록 구현
- 상단 요약 카드(29 킬팻캡슐 진행중 — 킬캡3체험 22·킬캡3개월 6·킬캡1개월 1)에서
  이미 집계 로직이 있으니, 이 집계 기준과 동일한 분류 기준을 재사용할 것

## 3. 검증
1. 배지 색상이 3일체험/1개월/3개월 세 가지로 시각적으로 구분되는지 확인
2. 킬팻캡슐 필터 클릭 시 세부 필터 버튼 노출 확인
3. 세부 필터(3일체험/1개월/3개월) 각각 클릭 시 목록이 정확히 필터링되는지 확인
4. 다른 카테고리(탕약/환/첩약건보) 필터는 기존 그대로 정상 동작하는지 회귀 확인
5. 상단 요약 카드 숫자와 세부 필터 결과 건수가 일치하는지 확인 (예: 3일체험
   필터 시 목록 건수 = 요약카드의 "킬캡3체험 22")

## 4. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "치료처방 목록: 킬팻캡슐 배지 색상 구분 + 세부 필터(3일체험/1개월/3개월) 추가"
- git push

## 완료 후 보고
- 변경 파일
- 배지 색상 적용 스크린샷 또는 색상값
- 검증 5단계 결과
- git 커밋 해시