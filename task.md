# 작업지시서: 통계 대시보드 구조 재설계 (환자현황/재방문·충성도/누적 3섹션)

## 배경
- 현재 대시보드는 "사람 수"와 "내원 건수"가 지표마다 기준이 뒤섞여 있어 원장님이 실사용 중 지속적으로 혼란을 겪음
- 구글시트보다 연속적 통계분석이 더 잘 되어야 한다는 게 이 툴의 존재 이유인데, 현재는 오히려 그 목적에 못 미침 — 이번엔 라벨 수정 수준이 아니라 구조 재설계로 진행

## 1단계: 조사 (구현 전 먼저 확인, 결과 보고 후 승인받고 진행)

1. 현재 대시보드에 존재하는 모든 지표를 나열하고, 각각이 "사람(중복제거 실인원)" 기준인지 "건수(내원체크 row)" 기준인지 표로 정리해서 보고
2. 초진/재초진/재진 등 진료구분 값이 실제 코드에서 어떻게 판정되는지 확인 (예: 같은 환자가 "초진" 태그를 두 번 받을 수 있는 구조인지 — 이게 위 대화에서 나온 "월별 신규환자수"와 "이번달 초진 건수"가 다른 숫자가 나올 수 있는 원인일 가능성 있음, 실제로 얼마나 차이나는지 현재 데이터로 대조 확인)
3. 7일 재방문율/3회이상 재방문율의 계산 기준(코호트 방식 — 초진 시점 기준으로 추적하는 지표라 "기간선택"과 안 맞을 수 있음)이 맞는지 재확인
4. Chart.js/Recharts 등 현재 콤보차트 라이브러리에서 tooltip 라벨이 시리즈 name 속성을 못 받아온 이유(버그 원인) 확인

## 2단계: 3섹션 재구조화

### 섹션 A. "환자 현황" (신규 — 기간선택 드롭다운 하나로 통일: 최근7일/30일/이번달)
- 초진 환자수 (건수 기준, 해당 기간 내)
- 재초진 환자수
- 재진 환자수
- 실인원(중복제거) — 해당 기간에 최소 1회 이상 내원한 고유 환자 수
- **1인당 평균 내원횟수** = 기간 내 총 내원건수 ÷ 실인원 (신규 지표, 이게 "재진들은 총 몇번 오는가"에 대한 답)
- 진료구분은 커스텀 항목 그대로 반영, 0건은 숨김 (기존 원칙 유지)
- 기존에 있던 "예약율" "인당내원수"(기간선택 드롭다운 포함) 카드도 이 섹션 안으로 이동 — 같은 기간선택 하나를 공유하되, 카드별 독립선택 유지 여부는 조사 결과 보고 후 결정

### 섹션 B. "재방문 / 충성도" (기존 유지, 이름과 설명만 명확화)
- 7일 재방문율, 3회이상 재방문율 유지
- 각 지표 옆에 짧은 설명 아이콘/툴팁 추가 — "초진 후 7일 내 재방문한 환자 비율" 처럼, 이게 "기간선택과 무관한 코호트 지표"라는 걸 명확히 표기
- 기간선택 드롭다운과 시각적으로 분리되어 있어야 함 (헷갈리지 않게 섹션 경계 명확히)

### 섹션 C. "누적 현황" (전체 기간 고정)
- 전체 등록환자수 (지금처럼 그대로, "등록"이라는 라벨 명확히 유지)
- 실제 내원이력 있는 누적환자수 (현재 304명 — "등록"과 다른 개념이라는 걸 라벨로 구분: 예 "실내원 누적환자수")
- 월별 신규·누적 콤보차트 유지, 아래 3~4단계 버그 수정 반영

## 3단계: 기존 버그 수정

1. 월별 신규·누적 콤보차트 툴팁 — 막대(신규환자수)와 선(누적환자수)의 name을 각각 명시적으로 지정해 라벨 정정
2. 콤보차트에 범례(legend) 추가 — 회색 막대="신규환자수(첫내원 기준)", 빨간선="누적 총환자수"
3. "이번달 일별 내원수·예약율 추이" 차트 툴팁 — 예약율(%) 시리즈가 "내원수"로 잘못 표시되는 라벨 버그 수정 (오른쪽 축 %, 왼쪽 축 명 단위로 각각 정확한 name 지정)
4. 두 콤보차트 모두, 이중축(왼쪽/오른쪽 단위 다름) 사용 중이라는 걸 사용자가 인지할 수 있게 축 제목("명" / "%") 명시

## 주의사항
- 기존 계산 로직(computeDashboardStats, computeMonthlyDailyStats 등) 자체 함수는 대조 확인 후 필요한 것만 재사용/확장, 불필요한 중복 계산 로직 생성 금지
- 스키마 변경 없음 (전부 기존 데이터 기반 집계 방식 변경)
- 브라우저에서 각 섹션 실제 숫자를 원본 데이터(내원체크 리스트)와 직접 대조해 정확성 재검증 — 특히 "초진 환자수(건수)"와 "월별 신규환자수(사람)"가 왜 다른지/같은지 결과에 반드시 코멘트 남길 것
- tsc/build 통과 확인 → 커밋/푸시 전 스크린샷으로 3섹션 레이아웃 확인 요청