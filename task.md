# 작업 지시서 — 공통 기본 공유 이미지(로고) 반영 + OG 이미지 우선순위 확정

## 배경
지금까지 오픈그래프(OG) 이미지가 페이지별로 들쭉날쭉하거나 없는 경우가 있었음.
원장님이 준비하신 규림한의원 정식 로고 이미지(첨부 파일)를 시스템 전체의
"기본 공유 이미지"로 쓰고, 개별 페이지가 자체 이미지(예: 킬팻캡슐 히어로 이미지)를
가지고 있으면 그걸 우선 사용하도록 확정.

## 작업 범위

### 1. 로고 이미지 반영
- 첨부된 로고 이미지 파일을 프로젝트에 저장 (예: `public/og/gyurim-default.png`
  또는 기존 정적 자산 관리 방식에 맞는 위치 — Claude Code가 기존 구조 참고해서
  판단)
- 이미지 비율이 이미 OG 표준(1200x630에 가까운 가로형)에 준하니, 별도 리사이즈
  없이 그대로 사용 가능한지 확인하고, 필요하면 여백 비율만 최소 조정

### 2. OG 이미지 우선순위 로직 확정
아래 순서로 og:image 결정:
1. 페이지가 자체 히어로 이미지를 가지고 있으면(`TrialCampaignSettings` 등) 그것 사용
   — 예: `/refer/trial`, `/refer/trial/[token]`, `/refer/my/[token]`은 기존
   킬팻캡슐 히어로 이미지 그대로 유지
2. 자체 이미지가 없으면 위에서 저장한 공통 로고 이미지를 기본값으로 사용
   — 검사톡 결과 페이지, 마감설문(`/refer/exit/[prescriptionId]`), 기존
   `/s/[token]`, `/p/[token]` 등 전용 이미지가 없는 나머지 전부 해당
- 이 우선순위 판단 로직은 한 곳(공용 유틸/헬퍼)에 모아서, 새로운 공개 페이지가
  추가될 때마다 반복 구현 안 해도 되게 만들 것

### 3. 적용 대상 페이지 점검
- `/refer/exit/[prescriptionId]` (마감설문) — 지금 이미지 없이 텍스트만
  있었다면 이제 로고 이미지 적용
- `/s/[token]`, `/p/[token]` (기존 공유링크) — 전용 이미지 없는 경우 로고
  이미지로 폴백되는지 확인
- 검사톡 링크 페이지 — 동일하게 폴백 확인

## 검증
- 실제 카카오톡에 각 링크 종류(마감설문, `/s/`, 검사톡 등)를 붙여넣어 미리보기에
  로고 이미지가 정상 노출되는지 확인
- `/refer/trial`, `/refer/my`는 여전히 기존 히어로 이미지가 우선 적용되는지
  (로고로 잘못 덮이지 않는지) 확인
- npx tsc --noEmit / npm run build 통과

## 완료 후 보고
- 저장한 이미지 경로
- 수정 파일 경로
- 커밋 해시, git push 결과