# 버그 리포트 — /s/[token] 카드7 렌더링 크래시 (긴급)

## 증상
- URL: link.kyurim.kr/s/Y06C8IRw 접속 시 런타임 에러
- `Cannot read properties of undefined (reading 'split')`
- 콜스택: renderTreatmentBody (src_0283_jj._.js:752) ← Array.map ← HrvHealthReportCards (1049)

## 원인 파악 (먼저 할 것)
1. 이 토큰(Y06C8IRw)이 어느 환자/어느 검사 레코드인지 DB에서 직접 조회
2. `renderTreatmentBody`가 `.split()`을 호출하는 대상 필드가 정확히 무엇인지 확인
   (doctorText/patientText를 마커 문자열로 분리하는 로직으로 추정 — 정확한 필드명/
   마커 문자열 코드에서 확인)
3. 이번 세션에서 "레거시 null 레코드는 안전하게 처리됨"으로 검증했다고 보고했는데
   실제로는 크래시가 났음 — 테스트 커버리지에 없었던 제3의 상태가 뭔지 정확히 규명할 것.
   가능성:
   - aiTreatmentCardsJson 자체는 존재하지만 그 안의 특정 카테고리만 신규 포맷(마커 포함)이
     아니고 구버전 단일 텍스트 포맷인 "혼합 상태" 레코드
   - 신규 생성 시도했으나 AI 응답이 마커 포맷을 안 지켜서 파싱 실패한 채 그대로 저장된 레코드
   - 후보 카테고리가 0개(전 카테고리 무응답)인데도 렌더링 함수가 빈 배열이 아니라
     undefined를 순회하려 한 케이스
   원인을 추측으로 넘어가지 말고, 이 토큰의 실제 저장값을 콘솔/DB 조회로 직접 확인해서 규명할 것.

## 수정
- `renderTreatmentBody` 진입 시 대상 필드 존재 여부 guard 처리 — 없으면 해당 카테고리
  카드를 조용히 스킵(에러 아님), 전체 카드7 섹션이 죽지 않도록
- 위에서 규명한 원인이 "혼합 상태 레코드"라면, 혼합 상태도 정상 렌더링되도록 폴백 로직
  추가 (구버전 필드면 구버전 방식으로, 신버전 필드면 신버전 방식으로 카테고리 단위 분기)
- 원인이 "AI 응답이 마커 포맷을 안 지킨 채 저장된 레코드"라면, 저장 전 마커 파싱
  성공 여부를 검증하는 안전장치를 생성 파이프라인에 추가 (파싱 실패 시 저장 자체를
  막고 재시도 — 기존 다른 안전장치들과 동일 원칙)

## 검증
- 이 토큰(Y06C8IRw)으로 직접 재현 후 수정 확인
- 최승희님 등 이미 검증했던 정상 케이스 회귀 없는지 재확인
- DB 내 다른 모든 활성 레코드에 대해서도 이 필드 상태를 스캔해서, 같은 크래시가 날 만한
  레코드가 몇 건 더 있는지 미리 파악해서 보고할 것 (사후 발견 방지)
- npx tsc --noEmit / npm run build 통과
- 커밋만, push는 보류 (원장 브라우저 확인 후 진행)