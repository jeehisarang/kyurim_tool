완료 보고가 중간에 잘렸습니다. 아래 항목들 완료 여부와 상세 내용을 다시 알려주세요:

1. 상담모드(/consultation) 화면 — "증상 패턴 체크" 드롭다운 옵션이 실제로 추가됐는지, 
   선택 시 체크리스트 UI로 전환되는지
2. 공유링크(/s/[token]) 4번째 섹션 — 검사결과→상담설문→티칭→이벤트 순서로 
   실제 반영됐는지, 응답 있을 때만 노출되는 로직 확인
3. HRV AI 코멘트 생성 로직에 증상 패턴 프로필 연동됐는지 (tcmPatternMapJson과 병행 동작)
4. 검증체크리스트 항목들 (테스트환자 426 기준 실제 테스트 결과):
   - 원장실 12문항(실제 11문항) 응답 저장 및 점수 계산
   - 같은 달 재작성 시 UPDATE, 월 변경 시 INSERT 정상 동작
   - 공유링크에서 환자 직접 응답 가능 여부
   - HRV 코멘트에 반영 여부 회귀 테스트
5. tierLabel() 3단계 컷오프를 0%=낮음, 1~50%=일부확인, 51~100%=뚜렷함으로 
   수정 완료 여부 (아직이면 지금 반영해주세요)