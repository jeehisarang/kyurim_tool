# 작업지시서: 미션톡 발송관리 개선 (프로그램 구분 + 발송이력 추적)

## 배경
- 미션톡 기능 테스트 중 (김우석 대상 1건 발송 완료)
- 환자 32명 대상으로 확장 운영 예정, 여러 담당자가 분담 발송할 예정
- 현재 "미발송/발송됨" 상태값만 있어 프로그램 구분·중복발송 확인이 안 됨

## 1단계: 조사 (구현 전 먼저 확인)

1. 현재 미션톡 발송 관련 스키마 확인
   - 발송상태(미발송/발송됨)를 저장하는 테이블/필드가 무엇인지
   - 프로그램(킬캡3체험/1개월/3개월) 정보가 환자별로 어디서 조회되는지 (치료처방 테이블 참조 추정)
2. 기존 치료처방 화면에서 쓰는 프로그램별 뱃지 색상/스타일 컴포넌트 위치 확인 (재사용 목적)
3. 위 조사 결과를 먼저 보고 → 구현 방식 제안 후 진행 (스키마 변경 전 승인 요청)

## 2단계: 프로그램별 뱃지 + 필터 + 검색

- 미션톡 발송 리스트("킬팻캡슐 진행중 환자") 각 행에 프로그램별 뱃지 표시
  - 기존 치료처방 화면의 프로그램별 색상 체계 그대로 재사용 (신규 색상 정의 금지)
- 상단에 프로그램 필터 추가 (킬캡3체험/1개월/3개월 전체/개별 선택)
- 환자명 또는 차트번호로 검색 가능한 검색창 추가
- 필터+검색은 클라이언트 사이드 처리로 충분 (현재 규모 32명 기준, 추후 늘어나면 서버사이드 검토)

## 3단계: 발송이력 추적 (MissionSendLog)

### 스키마 (ADD COLUMN 원칙 준수, 신규 테이블 추가)
- 신규 테이블: `MissionSendLog`
  - id
  - patientId (환자 참조)
  - missionId 또는 missionContent (어떤 미션 문구였는지 — 나중에 추적용, 최소 미션 제목/식별자만이라도)
  - sentByStaffId (담당자, 기존 사용자식별 구조 재사용)
  - sentAt (발송 일시)
  - createdAt

### 동작
1. "문구 생성" 버튼 클릭 시점 = 발송 완료 시점으로 간주하지 말고, **"복사" 버튼 클릭 시점**에 로그 기록 (실제 카톡 발송 직전 행동이 복사이므로 — 문구만 생성하고 안 보낼 수도 있음을 감안)
2. 리스트 각 행에 해당 환자의 발송이력 요약을 텍스트로 노출
   - 형식 예: `발송 3회 · 최근 07/28 최승희`
   - 이력 없으면 기존처럼 "미발송" 표시
3. 중복발송 경고
   - 담당자가 "문구 생성" 클릭 시, 해당 환자에게 **당일 이미 발송(=복사) 이력이 있으면** 경고 배너/툴팁 노출
     - 예: "⚠️ 오늘 최승희님이 이미 발송했습니다 (07/28 14:32)"
   - 경고는 표시만 하고 진행은 막지 않음 (담당자 최종판단, 버튼 비활성화 금지)

## 주의사항
- 기존 6종 문구 소프트 비활성화 방식과 동일하게, 이번에도 데이터 삭제 없이 신규 테이블/컬럼 추가만
- 라이브 환자 데이터로 테스트 금지, 테스트 환자(ID 426) 또는 합성 데이터 사용 후 즉시 삭제
- 완료 후 브라우저에서 실제 필터/검색/경고 동작 시각 확인 → 커밋/푸시는 그 다음