# 작업 지시서 — 자유톡 프롬프트 오염 수정 (내원유도용 고정 클로징 문구 혼입 제거)

## 배경
자유톡(FREEFORM)으로 "추천링크 안내" 같은 임의 목적 문구를 생성했는데, 결과에
네이버예약링크 안내 같은 문구가 붙어 나옴. 이는 2일차/7일차 등 "내원 유도"
목적의 톡에서 쓰는 고정 클로징 멘트가 자유톡 생성 경로에도 공통으로 섞여
들어가고 있는 것으로 의심됨. 자유톡은 매번 목적이 달라지는 범용 도구이므로,
특정 카테고리(내원유도) 전용 문구가 기본으로 깔리면 안 됨.

## 1. 원인 확인
- src/lib/ai-message.ts의 generateFreeformMessageDraft 함수와, 이 함수가 호출하는
  공통 프롬프트 조각(시스템 프롬프트 템플릿, 공통 헬퍼, 클로징 멘트 삽입 로직 등)을
  전부 확인
- 다음을 구체적으로 확인:
  - 네이버예약링크 안내 문구가 어디서 삽입되는지(하드코딩된 문자열인지, 공통
    프롬프트 지시문으로 AI가 매번 생성하는 것인지)
  - 이 로직이 DAY2/DAY7 전용 함수에만 있어야 하는데 공유 함수/공통 프롬프트
    조각으로 옮겨져서 FREEFORM 경로까지 함께 타는 구조인지
- 원인 파악 후 다음 방식으로 분리:
  - 네이버예약 안내, 기타 "내원 유도" 관련 고정 지시문은 DAY2/DAY7 등 해당
    카테고리 함수 내부에만 남기고, 공통/공유 부분에서는 완전히 제거
  - FREEFORM 프롬프트는 순수하게 "담당자가 입력한 목적 + 환자 메모/이력
    참고 + 비문 방지 자체검토 + 분량 제한(400자)"만 지시하도록 유지, 다른
    카테고리의 목적성 지시문(내원유도, 예약안내 등)이 전혀 섞이지 않게 함

## 2. 검증
1. 원인이 된 코드 위치를 스크린샷/코드 인용으로 보고
2. 수정 후, "추천링크 안내" 목적으로 동일하게 생성 요청 → 네이버예약 관련
   문구 없이, 순수 소개링크 안내 내용만 나오는지 확인
3. 전혀 다른 목적(예: "생일 축하 문자 써줘", "명절 인사")으로도 생성해봐서
   엉뚱한 클로징 문구 없이 목적에 맞는 내용만 나오는지 확인
4. 기존 DAY2/DAY7 톡은 네이버예약 안내 문구가 여전히 정상적으로 포함되어
   나오는지 회귀 확인 (이번 수정으로 실수로 같이 빠지면 안 됨)

## 3. Git 커밋 및 푸시
- npm run build 통과 확인
- git add .
- git commit -m "자유톡: DAY2/7 전용 클로징 문구(네이버예약 등) 혼입 제거, 프롬프트 분리"
- git push

## 완료 후 보고
- 오염 원인 정확한 위치/코드 인용
- 수정 전/후 자유톡 생성 결과 비교 (추천링크 안내 목적 기준)
- 다른 목적(생일/명절 인사 등) 생성 테스트 결과
- 기존 DAY2/DAY7 회귀 확인 결과
- git 커밋 해시